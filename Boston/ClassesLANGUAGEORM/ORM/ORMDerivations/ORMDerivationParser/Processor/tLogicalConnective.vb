Namespace Derivations

    ''' <summary>
    ''' Object representation of the TinyPG LOGICAL_CONNECTIVE choice.
    ''' Exactly one keyword is populated for a valid parse node.
    ''' </summary>
    Public Class LogicalConnective

        Public Property KEYWD_WHERE_NEGATED As String = String.Empty
        Public Property KEYWD_WHERE As String = String.Empty
        Public Property KEYWD_AND As String = String.Empty
        Public Property KEYWD_OR As String = String.Empty

    End Class

End Namespace
