' Generated by TinyPG v1.3 available at www.codeproject.com

Imports System
Imports System.Collections.Generic


Namespace ORMDerivation
#Region "Parser"

    Partial Public Class Parser 
        Private m_scanner As Scanner
        Private m_tree As ParseTree
        Private max_tree As ParseTree
        Public MaxDistance As Integer 'The maximum distance the parser got within the input text.

        'To stop multiple threads parsing at once.
        Private LockObject As New Object

        Public Sub New(ByVal scanner As Scanner)
            m_scanner = scanner
        End Sub


        Public Function Parse(ByVal input As String) As ParseTree
            m_tree = New ParseTree()
            'Return Parse(input, m_tree)  '20210810-VM-Was this, changed to max_tree below
            max_tree = New ParseTree 'Added, as above.

            If System.Threading.Monitor.TryEnter(Me.LockObject) Then
                Try
                    Return Parse(input, max_tree) 'Added, as above.
                Finally
                    System.Threading.Monitor.Exit(Me.LockObject)
                End Try
            Else
                Return max_tree
            End If

        End Function

        Public Function Parse(ByVal input As String, ByVal tree As ParseTree) As ParseTree
            m_scanner.Init(input)
            MaxDistance = 0
            m_tree = tree
            ParseStart(m_tree)
            m_tree.Skipped = m_scanner.Skipped
            Me.max_tree.Errors = m_tree.Errors '20231225-VM-Added. Was not returning errors if first production failed.
            Return Me.max_tree 'm_tree '20210810-VM-Added max_tree, commented out m_tree
        End Function

                Private Function ParseDERIVATION(ByVal parent As ParseNode) As Boolean ' NonTerminalSymbol: DERIVATION
            Dim tok As Token
            Dim n As ParseNode
            Dim node As ParseNode = parent.CreateNode(m_scanner.GetToken(TokenType.DERIVATION), "DERIVATION")
            Dim lbProblemSolved As Boolean = True

            Dim liOriginalRange as Integer = m_scanner.StartPos
            Dim liMaxRange as Integer = liOriginalRange
            parent.Nodes.Add(node)

            Try

             ' Concat Rule
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.DERIVATION_MARKER) ' Terminal Rule: DERIVATION_MARKER
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.DERIVATION_MARKER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "DERIVATION_MARKER"))
                    End If
                    If tok.Type <> TokenType.DERIVATION_MARKER Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.DERIVATION_MARKER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "DERIVATION_MARKER"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.DERIVATION_MARKER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "DERIVATION_MARKER"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If

             ' Concat Rule
            lbProblemSolved =                     ParseDERIVATION_SENTENCE(node) ' NonTerminal Rule: DERIVATION_SENTENCE
                    If m_tree.Errors.Count > 0 Then
                      If m_scanner.EndPos > Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree = m_tree.clone
                      End If
                      lbProblemSolved = False
                    End If

               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If

             ' Concat Rule
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.FULL_STOP) ' Terminal Rule: FULL_STOP
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.FULL_STOP.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "FULL_STOP"))
                    End If
                    If tok.Type <> TokenType.FULL_STOP Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.FULL_STOP.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "FULL_STOP"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.FULL_STOP.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "FULL_STOP"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If
               If m_tree.Errors.Count > 0 Then
                  Return False
               End If

            If m_scanner.Input.Length > (parent.Token.EndPos + 1) Then
            End If
            Finally
                If lbProblemSolved Then
                    parent.Token.UpdateRange(node.Token)
                    Me.MaxDistance = node.Token.EndPos
                    If m_scanner.EndPos >= Me.MaxDistance Then
                       If m_tree.MaxDistance > max_tree.MaxDistance Then
                          Me.MaxDistance = m_scanner.StartPos
                          max_tree = m_tree.clone
                       End If
                    End If
                Else
                   m_scanner.StartPos = liOriginalRange
                   parent.Nodes.Remove(node)
                End If
            End Try
            Return lbProblemSolved
        End Function ' NonTerminalSymbol: DERIVATION

        Private Function ParseDERIVATION_SENTENCE(ByVal parent As ParseNode) As Boolean ' NonTerminalSymbol: DERIVATION_SENTENCE
            Dim tok As Token
            Dim n As ParseNode
            Dim node As ParseNode = parent.CreateNode(m_scanner.GetToken(TokenType.DERIVATION_SENTENCE), "DERIVATION_SENTENCE")
            Dim lbProblemSolved As Boolean = True

            Dim liOriginalRange as Integer = m_scanner.StartPos
            Dim liMaxRange as Integer = liOriginalRange
            parent.Nodes.Add(node)

            Try
            tok = m_scanner.LookAhead({TokenType.KEYWD_EACH, TokenType.MODEL_ELEMENT_NAME}) ' Choice Rule
            
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_EACH.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_EACH"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_EACH.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "MODEL_ELEMENT_NAME"))
            Select Case tok.Type
             ' Choice Rule
                Case TokenType.KEYWD_EACH
            lbProblemSolved =                     ParseDEFINITION_DERIVATION(node) ' NonTerminal Rule: DEFINITION_DERIVATION
                    If m_tree.Errors.Count > 0 Then
                      If m_scanner.EndPos > Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree = m_tree.clone
                      End If
                      lbProblemSolved = False
                    End If

                Case TokenType.MODEL_ELEMENT_NAME
            lbProblemSolved =                     ParseBICONDITIONAL_DERIVATION(node) ' NonTerminal Rule: BICONDITIONAL_DERIVATION
                    If m_tree.Errors.Count > 0 Then
                      If m_scanner.EndPos > Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree = m_tree.clone
                      End If
                      lbProblemSolved = False
                    End If

                Case Else
                If m_tree.Errors.Count = 0 Then
                lbProblemSolved = False
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_EACH.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_EACH"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_EACH.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "MODEL_ELEMENT_NAME"))
                End If
                    m_tree.Errors.Add(new ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found.", &H0002, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos))
                    Exit Select
            End Select ' Choice Rule
                If Not lbProblemSolved Then
                   m_tree.Errors.Clear
                If Not lbProblemSolved Then
                  m_tree.Errors.Clear
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
            lbProblemSolved =                 ParseBICONDITIONAL_DERIVATION(parent.Nodes(parent.Nodes.Count -1)) ' NonTerminal Rule: BICONDITIONAL_DERIVATION
                If m_tree.Errors.Count > 0 Then
                  If m_scanner.EndPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  lbProblemSolved = False
                Else If m_scanner.EndPos = Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.EndPos
                    max_tree = m_tree.clone
                End If

                End If
                End If
             If (m_tree.Errors.Count > 0) Or Not lbProblemSolved Then
                 parent.Nodes.Remove(node)
                 Return False
             End If

            If m_scanner.Input.Length > (parent.Token.EndPos + 1) Then
            End If
            Finally
                If lbProblemSolved Then
                    parent.Token.UpdateRange(node.Token)
                    Me.MaxDistance = node.Token.EndPos
                    If m_scanner.EndPos >= Me.MaxDistance Then
                       If m_tree.MaxDistance > max_tree.MaxDistance Then
                          Me.MaxDistance = m_scanner.StartPos
                          max_tree = m_tree.clone
                       End If
                    End If
                Else
                   m_scanner.StartPos = liOriginalRange
                   parent.Nodes.Remove(node)
                End If
            End Try
            Return lbProblemSolved
        End Function ' NonTerminalSymbol: DERIVATION_SENTENCE

        Private Function ParseDEFINITION_DERIVATION(ByVal parent As ParseNode) As Boolean ' NonTerminalSymbol: DEFINITION_DERIVATION
            Dim tok As Token
            Dim n As ParseNode
            Dim node As ParseNode = parent.CreateNode(m_scanner.GetToken(TokenType.DEFINITION_DERIVATION), "DEFINITION_DERIVATION")
            Dim lbProblemSolved As Boolean = True

            Dim liOriginalRange as Integer = m_scanner.StartPos
            Dim liMaxRange as Integer = liOriginalRange
            parent.Nodes.Add(node)

            Try

             ' Concat Rule
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.KEYWD_EACH) ' Terminal Rule: KEYWD_EACH
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_EACH.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_EACH"))
                    End If
                    If tok.Type <> TokenType.KEYWD_EACH Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_EACH.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_EACH"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_EACH.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_EACH"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If

             ' Concat Rule
            lbProblemSolved =                     ParseDERIVATION_EXPRESSION(node) ' NonTerminal Rule: DERIVATION_EXPRESSION
                    If m_tree.Errors.Count > 0 Then
                      If m_scanner.EndPos > Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree = m_tree.clone
                      End If
                      lbProblemSolved = False
                    End If

               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If
               If m_tree.Errors.Count > 0 Then
                  Return False
               End If

            If m_scanner.Input.Length > (parent.Token.EndPos + 1) Then
            End If
            Finally
                If lbProblemSolved Then
                    parent.Token.UpdateRange(node.Token)
                    Me.MaxDistance = node.Token.EndPos
                    If m_scanner.EndPos >= Me.MaxDistance Then
                       If m_tree.MaxDistance > max_tree.MaxDistance Then
                          Me.MaxDistance = m_scanner.StartPos
                          max_tree = m_tree.clone
                       End If
                    End If
                Else
                   m_scanner.StartPos = liOriginalRange
                   parent.Nodes.Remove(node)
                End If
            End Try
            Return lbProblemSolved
        End Function ' NonTerminalSymbol: DEFINITION_DERIVATION

        Private Function ParseBICONDITIONAL_DERIVATION(ByVal parent As ParseNode) As Boolean ' NonTerminalSymbol: BICONDITIONAL_DERIVATION
            Dim tok As Token
            Dim n As ParseNode
            Dim node As ParseNode = parent.CreateNode(m_scanner.GetToken(TokenType.BICONDITIONAL_DERIVATION), "BICONDITIONAL_DERIVATION")
            Dim lbProblemSolved As Boolean = True

            Dim liOriginalRange as Integer = m_scanner.StartPos
            Dim liMaxRange as Integer = liOriginalRange
            parent.Nodes.Add(node)

            Try

             ' Concat Rule
            lbProblemSolved =                     ParseDERIVATION_HEAD_READING(node) ' NonTerminal Rule: DERIVATION_HEAD_READING
                    If m_tree.Errors.Count > 0 Then
                      If m_scanner.EndPos > Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree = m_tree.clone
                      End If
                      lbProblemSolved = False
                    End If

               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If

             ' Concat Rule
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.KEYWD_IF_AND_ONLY_IF) ' Terminal Rule: KEYWD_IF_AND_ONLY_IF
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_IF_AND_ONLY_IF.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_IF_AND_ONLY_IF"))
                    End If
                    If tok.Type <> TokenType.KEYWD_IF_AND_ONLY_IF Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_IF_AND_ONLY_IF.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_IF_AND_ONLY_IF"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_IF_AND_ONLY_IF.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_IF_AND_ONLY_IF"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If

             ' Concat Rule
            lbProblemSolved =                     ParseDERIVATION_EXPRESSION(node) ' NonTerminal Rule: DERIVATION_EXPRESSION
                    If m_tree.Errors.Count > 0 Then
                      If m_scanner.EndPos > Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree = m_tree.clone
                      End If
                      lbProblemSolved = False
                    End If

               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If
               If m_tree.Errors.Count > 0 Then
                  Return False
               End If

            If m_scanner.Input.Length > (parent.Token.EndPos + 1) Then
            End If
            Finally
                If lbProblemSolved Then
                    parent.Token.UpdateRange(node.Token)
                    Me.MaxDistance = node.Token.EndPos
                    If m_scanner.EndPos >= Me.MaxDistance Then
                       If m_tree.MaxDistance > max_tree.MaxDistance Then
                          Me.MaxDistance = m_scanner.StartPos
                          max_tree = m_tree.clone
                       End If
                    End If
                Else
                   m_scanner.StartPos = liOriginalRange
                   parent.Nodes.Remove(node)
                End If
            End Try
            Return lbProblemSolved
        End Function ' NonTerminalSymbol: BICONDITIONAL_DERIVATION

        Private Function ParseDERIVATION_HEAD_READING(ByVal parent As ParseNode) As Boolean ' NonTerminalSymbol: DERIVATION_HEAD_READING
            Dim tok As Token
            Dim n As ParseNode
            Dim node As ParseNode = parent.CreateNode(m_scanner.GetToken(TokenType.DERIVATION_HEAD_READING), "DERIVATION_HEAD_READING")
            Dim lbProblemSolved As Boolean = True

            Dim liOriginalRange as Integer = m_scanner.StartPos
            Dim liMaxRange as Integer = liOriginalRange
            parent.Nodes.Add(node)

            Try

             ' Concat Rule
            lbProblemSolved =                     ParseMODEL_ELEMENT_REFERENCE(node) ' NonTerminal Rule: MODEL_ELEMENT_REFERENCE
                    If m_tree.Errors.Count > 0 Then
                      If m_scanner.EndPos > Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree = m_tree.clone
                      End If
                      lbProblemSolved = False
                    End If

               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If

             ' Concat Rule
                                tok = m_scanner.LookAhead(TokenType.KEYWD_SOME, TokenType.KEYWD_EACH, TokenType.KEYWD_NO, TokenType.KEYWD_THAT, TokenType.KEYWD_POSSIBLE_VALUES_OF, TokenType.KEYWD_BY_DEFINITION, TokenType.KEYWD_AT_LEAST, TokenType.KEYWD_AT_MOST, TokenType.FUNCTION_NAME, TokenType.OPEN_PARENTHESIS, TokenType.COMPARISON_OPERATOR, TokenType.ARITHMETIC_OPERATOR, TokenType.NUMBER_LITERAL, TokenType.TEXT_LITERAL, TokenType.COMMA_SEPARATOR, TokenType.MODEL_ELEMENT_NAME, TokenType.PREDICATE_WORD) ' ZeroOrMore Rule
                    While tok.Type = TokenType.KEYWD_SOME Or tok.Type = TokenType.KEYWD_EACH Or tok.Type = TokenType.KEYWD_NO Or tok.Type = TokenType.KEYWD_THAT Or tok.Type = TokenType.KEYWD_POSSIBLE_VALUES_OF Or tok.Type = TokenType.KEYWD_BY_DEFINITION Or tok.Type = TokenType.KEYWD_AT_LEAST Or tok.Type = TokenType.KEYWD_AT_MOST Or tok.Type = TokenType.FUNCTION_NAME Or tok.Type = TokenType.OPEN_PARENTHESIS Or tok.Type = TokenType.COMPARISON_OPERATOR Or tok.Type = TokenType.ARITHMETIC_OPERATOR Or tok.Type = TokenType.NUMBER_LITERAL Or tok.Type = TokenType.TEXT_LITERAL Or tok.Type = TokenType.COMMA_SEPARATOR Or tok.Type = TokenType.MODEL_ELEMENT_NAME Or tok.Type = TokenType.PREDICATE_WORD
                    m_tree.Errors.Clear
                        ParseFACT_TYPE_PATH_COMPONENT(node) ' NonTerminal Rule: FACT_TYPE_PATH_COMPONENT
                        If m_tree.Errors.Count > 0 Then
                          If m_scanner.EndPos > Me.MaxDistance Then
                            Me.MaxDistance = m_scanner.StartPos
                            max_tree = m_tree.clone
                          End If
                          lbProblemSolved = False
                        End If
                    tok = m_scanner.LookAhead(TokenType.KEYWD_SOME, TokenType.KEYWD_EACH, TokenType.KEYWD_NO, TokenType.KEYWD_THAT, TokenType.KEYWD_POSSIBLE_VALUES_OF, TokenType.KEYWD_BY_DEFINITION, TokenType.KEYWD_AT_LEAST, TokenType.KEYWD_AT_MOST, TokenType.FUNCTION_NAME, TokenType.OPEN_PARENTHESIS, TokenType.COMPARISON_OPERATOR, TokenType.ARITHMETIC_OPERATOR, TokenType.NUMBER_LITERAL, TokenType.TEXT_LITERAL, TokenType.COMMA_SEPARATOR, TokenType.MODEL_ELEMENT_NAME, TokenType.PREDICATE_WORD) ' ZeroOrMore Rule
                    If Not lbProblemSolved Then Exit While
                    End While
            If m_tree.Errors.Count > 0 Then
                                Return False
            End If

               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If
               If m_tree.Errors.Count > 0 Then
                  Return False
               End If

            If m_scanner.Input.Length > (parent.Token.EndPos + 1) Then
            End If
            Finally
                If lbProblemSolved Then
                    parent.Token.UpdateRange(node.Token)
                    Me.MaxDistance = node.Token.EndPos
                    If m_scanner.EndPos >= Me.MaxDistance Then
                       If m_tree.MaxDistance > max_tree.MaxDistance Then
                          Me.MaxDistance = m_scanner.StartPos
                          max_tree = m_tree.clone
                       End If
                    End If
                Else
                   m_scanner.StartPos = liOriginalRange
                   parent.Nodes.Remove(node)
                End If
            End Try
            Return lbProblemSolved
        End Function ' NonTerminalSymbol: DERIVATION_HEAD_READING

        Private Function ParseMODEL_ELEMENT_REFERENCE(ByVal parent As ParseNode) As Boolean ' NonTerminalSymbol: MODEL_ELEMENT_REFERENCE
            Dim tok As Token
            Dim n As ParseNode
            Dim node As ParseNode = parent.CreateNode(m_scanner.GetToken(TokenType.MODEL_ELEMENT_REFERENCE), "MODEL_ELEMENT_REFERENCE")
            Dim lbProblemSolved As Boolean = True

            Dim liOriginalRange as Integer = m_scanner.StartPos
            Dim liMaxRange as Integer = liOriginalRange
            parent.Nodes.Add(node)

            Try

             ' Concat Rule
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.MODEL_ELEMENT_NAME) ' Terminal Rule: MODEL_ELEMENT_NAME
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.MODEL_ELEMENT_NAME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "MODEL_ELEMENT_NAME"))
                    End If
                    If tok.Type <> TokenType.MODEL_ELEMENT_NAME Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.MODEL_ELEMENT_NAME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "MODEL_ELEMENT_NAME"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.MODEL_ELEMENT_NAME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "MODEL_ELEMENT_NAME"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If

             ' Concat Rule
                                tok = m_scanner.LookAhead({TokenType.MODEL_ELEMENT_ALIAS_NUMBER}) ' Option Rule
                    If m_scanner.EndPos >= Me.MaxDistance Then
                                            max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.MODEL_ELEMENT_ALIAS_NUMBER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "MODEL_ELEMENT_ALIAS_NUMBER"))
                    End If
                    If tok.Type = TokenType.MODEL_ELEMENT_ALIAS_NUMBER Then
                        lbProblemSolved = True
                                                tok = m_scanner.Scan(TokenType.MODEL_ELEMENT_ALIAS_NUMBER) ' Terminal Rule: MODEL_ELEMENT_ALIAS_NUMBER
                        n = node.CreateNode(tok, tok.ToString() )
                        node.Token.UpdateRange(tok)
                        node.Nodes.Add(n)
                        If m_scanner.StartPos >= Me.MaxDistance Then
                          m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.MODEL_ELEMENT_ALIAS_NUMBER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "MODEL_ELEMENT_ALIAS_NUMBER"))
                        End If
                        If tok.Type <> TokenType.MODEL_ELEMENT_ALIAS_NUMBER Then
                          m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.MODEL_ELEMENT_ALIAS_NUMBER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "MODEL_ELEMENT_ALIAS_NUMBER"))
                          lbProblemSolved = False
                          If liMaxRange >= Me.MaxDistance Then
                            Me.MaxDistance = m_scanner.StartPos
                            max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.MODEL_ELEMENT_ALIAS_NUMBER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "MODEL_ELEMENT_ALIAS_NUMBER"))
                            max_tree = m_tree.clone
                          End If
                          Return False

                        Else
                        If m_scanner.StartPos >= Me.MaxDistance Then
                            m_tree.Optionals.Clear
                        End If
                        End If


                    Else
                                            m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.MODEL_ELEMENT_ALIAS_NUMBER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "MODEL_ELEMENT_ALIAS_NUMBER"))
                    End If
                    If m_tree.Errors.Count > 0 Then
                      Return False
                    End If

               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If
               If m_tree.Errors.Count > 0 Then
                  Return False
               End If

            If m_scanner.Input.Length > (parent.Token.EndPos + 1) Then
            End If
            Finally
                If lbProblemSolved Then
                    parent.Token.UpdateRange(node.Token)
                    Me.MaxDistance = node.Token.EndPos
                    If m_scanner.EndPos >= Me.MaxDistance Then
                       If m_tree.MaxDistance > max_tree.MaxDistance Then
                          Me.MaxDistance = m_scanner.StartPos
                          max_tree = m_tree.clone
                       End If
                    End If
                Else
                   m_scanner.StartPos = liOriginalRange
                   parent.Nodes.Remove(node)
                End If
            End Try
            Return lbProblemSolved
        End Function ' NonTerminalSymbol: MODEL_ELEMENT_REFERENCE

        Private Function ParseDERIVATION_EXPRESSION(ByVal parent As ParseNode) As Boolean ' NonTerminalSymbol: DERIVATION_EXPRESSION
            Dim tok As Token
            Dim n As ParseNode
            Dim node As ParseNode = parent.CreateNode(m_scanner.GetToken(TokenType.DERIVATION_EXPRESSION), "DERIVATION_EXPRESSION")
            Dim lbProblemSolved As Boolean = True

            Dim liOriginalRange as Integer = m_scanner.StartPos
            Dim liMaxRange as Integer = liOriginalRange
            parent.Nodes.Add(node)

            Try

             ' Concat Rule
            lbProblemSolved =                     ParseDERIVATION_CLAUSE(node) ' NonTerminal Rule: DERIVATION_CLAUSE
                    If m_tree.Errors.Count > 0 Then
                      If m_scanner.EndPos > Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree = m_tree.clone
                      End If
                      lbProblemSolved = False
                    End If

               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If

             ' Concat Rule
                                tok = m_scanner.LookAhead(TokenType.KEYWD_WHERE_NEGATED, TokenType.KEYWD_WHERE, TokenType.KEYWD_AND, TokenType.KEYWD_OR) ' ZeroOrMore Rule
                    While tok.Type = TokenType.KEYWD_WHERE_NEGATED Or tok.Type = TokenType.KEYWD_WHERE Or tok.Type = TokenType.KEYWD_AND Or tok.Type = TokenType.KEYWD_OR
                    m_tree.Errors.Clear

                         ' Concat Rule
                        lbProblemSolved =                                 ParseLOGICAL_CONNECTIVE(node) ' NonTerminal Rule: LOGICAL_CONNECTIVE
                                If m_tree.Errors.Count > 0 Then
                                  If m_scanner.EndPos > Me.MaxDistance Then
                                    Me.MaxDistance = m_scanner.StartPos
                                    max_tree = m_tree.clone
                                  End If
                                  lbProblemSolved = False
                                End If

                           If Not lbProblemSolved Then
                              If m_scanner.StartPos > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                              m_scanner.StartPos = liMaxRange
                              Return False
                           Else
                              liMaxRange = m_scanner.EndPos
                              If liMaxRange > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                           End If

                         ' Concat Rule
                        lbProblemSolved =                                 ParseDERIVATION_CLAUSE(node) ' NonTerminal Rule: DERIVATION_CLAUSE
                                If m_tree.Errors.Count > 0 Then
                                  If m_scanner.EndPos > Me.MaxDistance Then
                                    Me.MaxDistance = m_scanner.StartPos
                                    max_tree = m_tree.clone
                                  End If
                                  lbProblemSolved = False
                                End If

                           If Not lbProblemSolved Then
                              If m_scanner.StartPos > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                              m_scanner.StartPos = liMaxRange
                              Return False
                           Else
                              liMaxRange = m_scanner.EndPos
                              If liMaxRange > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                           End If
                           If m_tree.Errors.Count > 0 Then
                              Return False
                           End If
                    tok = m_scanner.LookAhead(TokenType.KEYWD_WHERE_NEGATED, TokenType.KEYWD_WHERE, TokenType.KEYWD_AND, TokenType.KEYWD_OR) ' ZeroOrMore Rule
                    If Not lbProblemSolved Then Exit While
                    End While
            If m_tree.Errors.Count > 0 Then
                                Return False
            End If

               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If
               If m_tree.Errors.Count > 0 Then
                  Return False
               End If

            If m_scanner.Input.Length > (parent.Token.EndPos + 1) Then
            End If
            Finally
                If lbProblemSolved Then
                    parent.Token.UpdateRange(node.Token)
                    Me.MaxDistance = node.Token.EndPos
                    If m_scanner.EndPos >= Me.MaxDistance Then
                       If m_tree.MaxDistance > max_tree.MaxDistance Then
                          Me.MaxDistance = m_scanner.StartPos
                          max_tree = m_tree.clone
                       End If
                    End If
                Else
                   m_scanner.StartPos = liOriginalRange
                   parent.Nodes.Remove(node)
                End If
            End Try
            Return lbProblemSolved
        End Function ' NonTerminalSymbol: DERIVATION_EXPRESSION

        Private Function ParseLOGICAL_CONNECTIVE(ByVal parent As ParseNode) As Boolean ' NonTerminalSymbol: LOGICAL_CONNECTIVE
            Dim tok As Token
            Dim n As ParseNode
            Dim node As ParseNode = parent.CreateNode(m_scanner.GetToken(TokenType.LOGICAL_CONNECTIVE), "LOGICAL_CONNECTIVE")
            Dim lbProblemSolved As Boolean = True

            Dim liOriginalRange as Integer = m_scanner.StartPos
            Dim liMaxRange as Integer = liOriginalRange
            parent.Nodes.Add(node)

            Try
            tok = m_scanner.LookAhead({TokenType.KEYWD_WHERE_NEGATED, TokenType.KEYWD_WHERE, TokenType.KEYWD_AND, TokenType.KEYWD_OR}) ' Choice Rule
            
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_WHERE_NEGATED.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_WHERE_NEGATED"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_WHERE_NEGATED.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_WHERE"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_WHERE_NEGATED.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_AND"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_WHERE_NEGATED.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_OR"))
            Select Case tok.Type
             ' Choice Rule
                Case TokenType.KEYWD_WHERE_NEGATED
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.KEYWD_WHERE_NEGATED) ' Terminal Rule: KEYWD_WHERE_NEGATED
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_WHERE_NEGATED.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_WHERE_NEGATED"))
                    End If
                    If tok.Type <> TokenType.KEYWD_WHERE_NEGATED Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_WHERE_NEGATED.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_WHERE_NEGATED"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_WHERE_NEGATED.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_WHERE_NEGATED"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


                Case TokenType.KEYWD_WHERE
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.KEYWD_WHERE) ' Terminal Rule: KEYWD_WHERE
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_WHERE.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_WHERE"))
                    End If
                    If tok.Type <> TokenType.KEYWD_WHERE Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_WHERE.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_WHERE"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_WHERE.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_WHERE"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


                Case TokenType.KEYWD_AND
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.KEYWD_AND) ' Terminal Rule: KEYWD_AND
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_AND.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_AND"))
                    End If
                    If tok.Type <> TokenType.KEYWD_AND Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_AND.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_AND"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_AND.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_AND"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


                Case TokenType.KEYWD_OR
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.KEYWD_OR) ' Terminal Rule: KEYWD_OR
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_OR.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_OR"))
                    End If
                    If tok.Type <> TokenType.KEYWD_OR Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_OR.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_OR"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_OR.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_OR"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


                Case Else
                If m_tree.Errors.Count = 0 Then
                lbProblemSolved = False
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_WHERE_NEGATED.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_WHERE_NEGATED"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_WHERE_NEGATED.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_WHERE"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_WHERE_NEGATED.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_AND"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_WHERE_NEGATED.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_OR"))
                End If
                    m_tree.Errors.Add(new ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found.", &H0002, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos))
                    Exit Select
            End Select ' Choice Rule
                If Not lbProblemSolved Then
                   m_tree.Errors.Clear
                If Not lbProblemSolved Then
                  m_tree.Errors.Clear
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                        lbProblemSolved = True
                        tok = m_scanner.Scan(TokenType.KEYWD_WHERE) ' Terminal Rule: KEYWD_WHERE
            n = node.CreateNode(tok, tok.ToString() )
            node.Token.UpdateRange(tok)
            node.Nodes.Add(n)
            If m_scanner.StartPos >= Me.MaxDistance Then
              m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_WHERE.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_WHERE"))
            End If
            If tok.Type <> TokenType.KEYWD_WHERE Then
              m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_WHERE.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_WHERE"))
              lbProblemSolved = False
              If liMaxRange >= Me.MaxDistance Then
                Me.MaxDistance = m_scanner.StartPos
                max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_WHERE.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_WHERE"))
                max_tree = m_tree.clone
              End If
              Return False

            Else
            If m_scanner.StartPos >= Me.MaxDistance Then
                m_tree.Optionals.Clear
            End If
            End If


                End If
                If Not lbProblemSolved Then
                  m_tree.Errors.Clear
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                        lbProblemSolved = True
                        tok = m_scanner.Scan(TokenType.KEYWD_AND) ' Terminal Rule: KEYWD_AND
            n = node.CreateNode(tok, tok.ToString() )
            node.Token.UpdateRange(tok)
            node.Nodes.Add(n)
            If m_scanner.StartPos >= Me.MaxDistance Then
              m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_AND.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_AND"))
            End If
            If tok.Type <> TokenType.KEYWD_AND Then
              m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_AND.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_AND"))
              lbProblemSolved = False
              If liMaxRange >= Me.MaxDistance Then
                Me.MaxDistance = m_scanner.StartPos
                max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_AND.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_AND"))
                max_tree = m_tree.clone
              End If
              Return False

            Else
            If m_scanner.StartPos >= Me.MaxDistance Then
                m_tree.Optionals.Clear
            End If
            End If


                End If
                If Not lbProblemSolved Then
                  m_tree.Errors.Clear
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                        lbProblemSolved = True
                        tok = m_scanner.Scan(TokenType.KEYWD_OR) ' Terminal Rule: KEYWD_OR
            n = node.CreateNode(tok, tok.ToString() )
            node.Token.UpdateRange(tok)
            node.Nodes.Add(n)
            If m_scanner.StartPos >= Me.MaxDistance Then
              m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_OR.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_OR"))
            End If
            If tok.Type <> TokenType.KEYWD_OR Then
              m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_OR.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_OR"))
              lbProblemSolved = False
              If liMaxRange >= Me.MaxDistance Then
                Me.MaxDistance = m_scanner.StartPos
                max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_OR.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_OR"))
                max_tree = m_tree.clone
              End If
              Return False

            Else
            If m_scanner.StartPos >= Me.MaxDistance Then
                m_tree.Optionals.Clear
            End If
            End If


                End If
                End If
             If (m_tree.Errors.Count > 0) Or Not lbProblemSolved Then
                 parent.Nodes.Remove(node)
                 Return False
             End If

            If m_scanner.Input.Length > (parent.Token.EndPos + 1) Then
            End If
            Finally
                If lbProblemSolved Then
                    parent.Token.UpdateRange(node.Token)
                    Me.MaxDistance = node.Token.EndPos
                    If m_scanner.EndPos >= Me.MaxDistance Then
                       If m_tree.MaxDistance > max_tree.MaxDistance Then
                          Me.MaxDistance = m_scanner.StartPos
                          max_tree = m_tree.clone
                       End If
                    End If
                Else
                   m_scanner.StartPos = liOriginalRange
                   parent.Nodes.Remove(node)
                End If
            End Try
            Return lbProblemSolved
        End Function ' NonTerminalSymbol: LOGICAL_CONNECTIVE

        Private Function ParseDERIVATION_CLAUSE(ByVal parent As ParseNode) As Boolean ' NonTerminalSymbol: DERIVATION_CLAUSE
            Dim tok As Token
            Dim n As ParseNode
            Dim node As ParseNode = parent.CreateNode(m_scanner.GetToken(TokenType.DERIVATION_CLAUSE), "DERIVATION_CLAUSE")
            Dim lbProblemSolved As Boolean = True

            Dim liOriginalRange as Integer = m_scanner.StartPos
            Dim liMaxRange as Integer = liOriginalRange
            parent.Nodes.Add(node)

            Try

             ' Concat Rule
                                tok = m_scanner.LookAhead({TokenType.KEYWD_NEGATED}) ' Option Rule
                    If m_scanner.EndPos >= Me.MaxDistance Then
                                            max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_NEGATED.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_NEGATED"))
                    End If
                    If tok.Type = TokenType.KEYWD_NEGATED Then
                        lbProblemSolved = True
                                                tok = m_scanner.Scan(TokenType.KEYWD_NEGATED) ' Terminal Rule: KEYWD_NEGATED
                        n = node.CreateNode(tok, tok.ToString() )
                        node.Token.UpdateRange(tok)
                        node.Nodes.Add(n)
                        If m_scanner.StartPos >= Me.MaxDistance Then
                          m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_NEGATED.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_NEGATED"))
                        End If
                        If tok.Type <> TokenType.KEYWD_NEGATED Then
                          m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_NEGATED.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_NEGATED"))
                          lbProblemSolved = False
                          If liMaxRange >= Me.MaxDistance Then
                            Me.MaxDistance = m_scanner.StartPos
                            max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_NEGATED.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_NEGATED"))
                            max_tree = m_tree.clone
                          End If
                          Return False

                        Else
                        If m_scanner.StartPos >= Me.MaxDistance Then
                            m_tree.Optionals.Clear
                        End If
                        End If


                    Else
                                            m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_NEGATED.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_NEGATED"))
                    End If
                    If m_tree.Errors.Count > 0 Then
                      Return False
                    End If

               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If

             ' Concat Rule
            lbProblemSolved =                     ParseFACT_TYPE_PATH(node) ' NonTerminal Rule: FACT_TYPE_PATH
                    If m_tree.Errors.Count > 0 Then
                      If m_scanner.EndPos > Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree = m_tree.clone
                      End If
                      lbProblemSolved = False
                    End If

               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If
               If m_tree.Errors.Count > 0 Then
                  Return False
               End If

            If m_scanner.Input.Length > (parent.Token.EndPos + 1) Then
            End If
            Finally
                If lbProblemSolved Then
                    parent.Token.UpdateRange(node.Token)
                    Me.MaxDistance = node.Token.EndPos
                    If m_scanner.EndPos >= Me.MaxDistance Then
                       If m_tree.MaxDistance > max_tree.MaxDistance Then
                          Me.MaxDistance = m_scanner.StartPos
                          max_tree = m_tree.clone
                       End If
                    End If
                Else
                   m_scanner.StartPos = liOriginalRange
                   parent.Nodes.Remove(node)
                End If
            End Try
            Return lbProblemSolved
        End Function ' NonTerminalSymbol: DERIVATION_CLAUSE

        Private Function ParseFACT_TYPE_PATH(ByVal parent As ParseNode) As Boolean ' NonTerminalSymbol: FACT_TYPE_PATH
            Dim tok As Token
            Dim n As ParseNode
            Dim node As ParseNode = parent.CreateNode(m_scanner.GetToken(TokenType.FACT_TYPE_PATH), "FACT_TYPE_PATH")
            Dim lbProblemSolved As Boolean = True

            Dim liOriginalRange as Integer = m_scanner.StartPos
            Dim liMaxRange as Integer = liOriginalRange
            parent.Nodes.Add(node)

            Try
            Do ' OneOrMore Rule
                ParseFACT_TYPE_PATH_COMPONENT(node) ' NonTerminal Rule: FACT_TYPE_PATH_COMPONENT
                If m_tree.Errors.Count > 0 Then
                  If m_scanner.EndPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  lbProblemSolved = False
                End If
                tok = m_scanner.LookAhead(TokenType.KEYWD_SOME, TokenType.KEYWD_EACH, TokenType.KEYWD_NO, TokenType.KEYWD_THAT, TokenType.KEYWD_POSSIBLE_VALUES_OF, TokenType.KEYWD_BY_DEFINITION, TokenType.KEYWD_AT_LEAST, TokenType.KEYWD_AT_MOST, TokenType.FUNCTION_NAME, TokenType.OPEN_PARENTHESIS, TokenType.COMPARISON_OPERATOR, TokenType.ARITHMETIC_OPERATOR, TokenType.NUMBER_LITERAL, TokenType.TEXT_LITERAL, TokenType.COMMA_SEPARATOR, TokenType.MODEL_ELEMENT_NAME, TokenType.PREDICATE_WORD) ' OneOrMore Rule
            Loop While m_tree.Errors.Count = 0 And tok.Type = TokenType.KEYWD_SOME Or tok.Type = TokenType.KEYWD_EACH Or tok.Type = TokenType.KEYWD_NO Or tok.Type = TokenType.KEYWD_THAT Or tok.Type = TokenType.KEYWD_POSSIBLE_VALUES_OF Or tok.Type = TokenType.KEYWD_BY_DEFINITION Or tok.Type = TokenType.KEYWD_AT_LEAST Or tok.Type = TokenType.KEYWD_AT_MOST Or tok.Type = TokenType.FUNCTION_NAME Or tok.Type = TokenType.OPEN_PARENTHESIS Or tok.Type = TokenType.COMPARISON_OPERATOR Or tok.Type = TokenType.ARITHMETIC_OPERATOR Or tok.Type = TokenType.NUMBER_LITERAL Or tok.Type = TokenType.TEXT_LITERAL Or tok.Type = TokenType.COMMA_SEPARATOR Or tok.Type = TokenType.MODEL_ELEMENT_NAME Or tok.Type = TokenType.PREDICATE_WORD ' OneOrMore Rule
            If m_tree.Errors.Count > 0 Then
                        Return False
            End If

            If m_scanner.Input.Length > (parent.Token.EndPos + 1) Then
            End If
            Finally
                If lbProblemSolved Then
                    parent.Token.UpdateRange(node.Token)
                    Me.MaxDistance = node.Token.EndPos
                    If m_scanner.EndPos >= Me.MaxDistance Then
                       If m_tree.MaxDistance > max_tree.MaxDistance Then
                          Me.MaxDistance = m_scanner.StartPos
                          max_tree = m_tree.clone
                       End If
                    End If
                Else
                   m_scanner.StartPos = liOriginalRange
                   parent.Nodes.Remove(node)
                End If
            End Try
            Return lbProblemSolved
        End Function ' NonTerminalSymbol: FACT_TYPE_PATH

        Private Function ParseFACT_TYPE_PATH_COMPONENT(ByVal parent As ParseNode) As Boolean ' NonTerminalSymbol: FACT_TYPE_PATH_COMPONENT
            Dim tok As Token
            Dim n As ParseNode
            Dim node As ParseNode = parent.CreateNode(m_scanner.GetToken(TokenType.FACT_TYPE_PATH_COMPONENT), "FACT_TYPE_PATH_COMPONENT")
            Dim lbProblemSolved As Boolean = True

            Dim liOriginalRange as Integer = m_scanner.StartPos
            Dim liMaxRange as Integer = liOriginalRange
            parent.Nodes.Add(node)

            Try
            tok = m_scanner.LookAhead({TokenType.KEYWD_SOME, TokenType.KEYWD_EACH, TokenType.KEYWD_NO, TokenType.KEYWD_THAT, TokenType.KEYWD_POSSIBLE_VALUES_OF, TokenType.KEYWD_BY_DEFINITION, TokenType.KEYWD_AT_LEAST, TokenType.KEYWD_AT_MOST, TokenType.FUNCTION_NAME, TokenType.OPEN_PARENTHESIS, TokenType.COMPARISON_OPERATOR, TokenType.ARITHMETIC_OPERATOR, TokenType.NUMBER_LITERAL, TokenType.TEXT_LITERAL, TokenType.COMMA_SEPARATOR, TokenType.MODEL_ELEMENT_NAME, TokenType.PREDICATE_WORD}) ' Choice Rule
            
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_SOME"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_EACH"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_NO"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_THAT"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_POSSIBLE_VALUES_OF"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_BY_DEFINITION"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_AT_LEAST"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_AT_MOST"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "FUNCTION_NAME"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "OPEN_PARENTHESIS"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "COMPARISON_OPERATOR"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "ARITHMETIC_OPERATOR"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "NUMBER_LITERAL"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "TEXT_LITERAL"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "COMMA_SEPARATOR"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "MODEL_ELEMENT_NAME"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "PREDICATE_WORD"))
            Select Case tok.Type
             ' Choice Rule
                Case TokenType.KEYWD_SOME
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.KEYWD_SOME) ' Terminal Rule: KEYWD_SOME
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_SOME"))
                    End If
                    If tok.Type <> TokenType.KEYWD_SOME Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_SOME"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_SOME"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


                Case TokenType.KEYWD_EACH
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.KEYWD_EACH) ' Terminal Rule: KEYWD_EACH
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_EACH.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_EACH"))
                    End If
                    If tok.Type <> TokenType.KEYWD_EACH Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_EACH.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_EACH"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_EACH.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_EACH"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


                Case TokenType.KEYWD_NO
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.KEYWD_NO) ' Terminal Rule: KEYWD_NO
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_NO.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_NO"))
                    End If
                    If tok.Type <> TokenType.KEYWD_NO Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_NO.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_NO"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_NO.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_NO"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


                Case TokenType.KEYWD_THAT
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.KEYWD_THAT) ' Terminal Rule: KEYWD_THAT
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_THAT.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_THAT"))
                    End If
                    If tok.Type <> TokenType.KEYWD_THAT Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_THAT.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_THAT"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_THAT.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_THAT"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


                Case TokenType.KEYWD_POSSIBLE_VALUES_OF
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.KEYWD_POSSIBLE_VALUES_OF) ' Terminal Rule: KEYWD_POSSIBLE_VALUES_OF
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_POSSIBLE_VALUES_OF.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_POSSIBLE_VALUES_OF"))
                    End If
                    If tok.Type <> TokenType.KEYWD_POSSIBLE_VALUES_OF Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_POSSIBLE_VALUES_OF.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_POSSIBLE_VALUES_OF"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_POSSIBLE_VALUES_OF.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_POSSIBLE_VALUES_OF"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


                Case TokenType.KEYWD_BY_DEFINITION
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.KEYWD_BY_DEFINITION) ' Terminal Rule: KEYWD_BY_DEFINITION
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_BY_DEFINITION.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_BY_DEFINITION"))
                    End If
                    If tok.Type <> TokenType.KEYWD_BY_DEFINITION Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_BY_DEFINITION.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_BY_DEFINITION"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_BY_DEFINITION.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_BY_DEFINITION"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


                Case TokenType.KEYWD_AT_LEAST
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.KEYWD_AT_LEAST) ' Terminal Rule: KEYWD_AT_LEAST
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_AT_LEAST.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_AT_LEAST"))
                    End If
                    If tok.Type <> TokenType.KEYWD_AT_LEAST Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_AT_LEAST.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_AT_LEAST"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_AT_LEAST.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_AT_LEAST"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


                Case TokenType.KEYWD_AT_MOST
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.KEYWD_AT_MOST) ' Terminal Rule: KEYWD_AT_MOST
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_AT_MOST.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_AT_MOST"))
                    End If
                    If tok.Type <> TokenType.KEYWD_AT_MOST Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_AT_MOST.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_AT_MOST"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_AT_MOST.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_AT_MOST"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


                Case TokenType.FUNCTION_NAME
            lbProblemSolved =                     ParseFUNCTION_CALL(node) ' NonTerminal Rule: FUNCTION_CALL
                    If m_tree.Errors.Count > 0 Then
                      If m_scanner.EndPos > Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree = m_tree.clone
                      End If
                      lbProblemSolved = False
                    End If

                Case TokenType.OPEN_PARENTHESIS
            lbProblemSolved =                     ParsePARENTHESISED_DERIVATION_EXPRESSION(node) ' NonTerminal Rule: PARENTHESISED_DERIVATION_EXPRESSION
                    If m_tree.Errors.Count > 0 Then
                      If m_scanner.EndPos > Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree = m_tree.clone
                      End If
                      lbProblemSolved = False
                    End If

                Case TokenType.COMPARISON_OPERATOR
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.COMPARISON_OPERATOR) ' Terminal Rule: COMPARISON_OPERATOR
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.COMPARISON_OPERATOR.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "COMPARISON_OPERATOR"))
                    End If
                    If tok.Type <> TokenType.COMPARISON_OPERATOR Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.COMPARISON_OPERATOR.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "COMPARISON_OPERATOR"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.COMPARISON_OPERATOR.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "COMPARISON_OPERATOR"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


                Case TokenType.ARITHMETIC_OPERATOR
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.ARITHMETIC_OPERATOR) ' Terminal Rule: ARITHMETIC_OPERATOR
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.ARITHMETIC_OPERATOR.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "ARITHMETIC_OPERATOR"))
                    End If
                    If tok.Type <> TokenType.ARITHMETIC_OPERATOR Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.ARITHMETIC_OPERATOR.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "ARITHMETIC_OPERATOR"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.ARITHMETIC_OPERATOR.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "ARITHMETIC_OPERATOR"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


                Case TokenType.NUMBER_LITERAL
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.NUMBER_LITERAL) ' Terminal Rule: NUMBER_LITERAL
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.NUMBER_LITERAL.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "NUMBER_LITERAL"))
                    End If
                    If tok.Type <> TokenType.NUMBER_LITERAL Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.NUMBER_LITERAL.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "NUMBER_LITERAL"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.NUMBER_LITERAL.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "NUMBER_LITERAL"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


                Case TokenType.TEXT_LITERAL
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.TEXT_LITERAL) ' Terminal Rule: TEXT_LITERAL
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.TEXT_LITERAL.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "TEXT_LITERAL"))
                    End If
                    If tok.Type <> TokenType.TEXT_LITERAL Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.TEXT_LITERAL.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "TEXT_LITERAL"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.TEXT_LITERAL.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "TEXT_LITERAL"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


                Case TokenType.COMMA_SEPARATOR
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.COMMA_SEPARATOR) ' Terminal Rule: COMMA_SEPARATOR
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.COMMA_SEPARATOR.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "COMMA_SEPARATOR"))
                    End If
                    If tok.Type <> TokenType.COMMA_SEPARATOR Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.COMMA_SEPARATOR.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "COMMA_SEPARATOR"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.COMMA_SEPARATOR.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "COMMA_SEPARATOR"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


                Case TokenType.MODEL_ELEMENT_NAME
            lbProblemSolved =                     ParseMODEL_ELEMENT_REFERENCE(node) ' NonTerminal Rule: MODEL_ELEMENT_REFERENCE
                    If m_tree.Errors.Count > 0 Then
                      If m_scanner.EndPos > Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree = m_tree.clone
                      End If
                      lbProblemSolved = False
                    End If

                Case TokenType.PREDICATE_WORD
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.PREDICATE_WORD) ' Terminal Rule: PREDICATE_WORD
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.PREDICATE_WORD.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "PREDICATE_WORD"))
                    End If
                    If tok.Type <> TokenType.PREDICATE_WORD Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.PREDICATE_WORD.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "PREDICATE_WORD"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.PREDICATE_WORD.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "PREDICATE_WORD"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


                Case Else
                If m_tree.Errors.Count = 0 Then
                lbProblemSolved = False
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_SOME"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_EACH"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_NO"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_THAT"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_POSSIBLE_VALUES_OF"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_BY_DEFINITION"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_AT_LEAST"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_AT_MOST"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "FUNCTION_NAME"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "OPEN_PARENTHESIS"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "COMPARISON_OPERATOR"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "ARITHMETIC_OPERATOR"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "NUMBER_LITERAL"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "TEXT_LITERAL"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "COMMA_SEPARATOR"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "MODEL_ELEMENT_NAME"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "PREDICATE_WORD"))
                End If
                    m_tree.Errors.Add(new ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found.", &H0002, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos))
                    Exit Select
            End Select ' Choice Rule
                If Not lbProblemSolved Then
                   m_tree.Errors.Clear
                If Not lbProblemSolved Then
                  m_tree.Errors.Clear
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                        lbProblemSolved = True
                        tok = m_scanner.Scan(TokenType.KEYWD_EACH) ' Terminal Rule: KEYWD_EACH
            n = node.CreateNode(tok, tok.ToString() )
            node.Token.UpdateRange(tok)
            node.Nodes.Add(n)
            If m_scanner.StartPos >= Me.MaxDistance Then
              m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_EACH.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_EACH"))
            End If
            If tok.Type <> TokenType.KEYWD_EACH Then
              m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_EACH.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_EACH"))
              lbProblemSolved = False
              If liMaxRange >= Me.MaxDistance Then
                Me.MaxDistance = m_scanner.StartPos
                max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_EACH.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_EACH"))
                max_tree = m_tree.clone
              End If
              Return False

            Else
            If m_scanner.StartPos >= Me.MaxDistance Then
                m_tree.Optionals.Clear
            End If
            End If


                End If
                If Not lbProblemSolved Then
                  m_tree.Errors.Clear
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                        lbProblemSolved = True
                        tok = m_scanner.Scan(TokenType.KEYWD_NO) ' Terminal Rule: KEYWD_NO
            n = node.CreateNode(tok, tok.ToString() )
            node.Token.UpdateRange(tok)
            node.Nodes.Add(n)
            If m_scanner.StartPos >= Me.MaxDistance Then
              m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_NO.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_NO"))
            End If
            If tok.Type <> TokenType.KEYWD_NO Then
              m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_NO.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_NO"))
              lbProblemSolved = False
              If liMaxRange >= Me.MaxDistance Then
                Me.MaxDistance = m_scanner.StartPos
                max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_NO.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_NO"))
                max_tree = m_tree.clone
              End If
              Return False

            Else
            If m_scanner.StartPos >= Me.MaxDistance Then
                m_tree.Optionals.Clear
            End If
            End If


                End If
                If Not lbProblemSolved Then
                  m_tree.Errors.Clear
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                        lbProblemSolved = True
                        tok = m_scanner.Scan(TokenType.KEYWD_THAT) ' Terminal Rule: KEYWD_THAT
            n = node.CreateNode(tok, tok.ToString() )
            node.Token.UpdateRange(tok)
            node.Nodes.Add(n)
            If m_scanner.StartPos >= Me.MaxDistance Then
              m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_THAT.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_THAT"))
            End If
            If tok.Type <> TokenType.KEYWD_THAT Then
              m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_THAT.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_THAT"))
              lbProblemSolved = False
              If liMaxRange >= Me.MaxDistance Then
                Me.MaxDistance = m_scanner.StartPos
                max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_THAT.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_THAT"))
                max_tree = m_tree.clone
              End If
              Return False

            Else
            If m_scanner.StartPos >= Me.MaxDistance Then
                m_tree.Optionals.Clear
            End If
            End If


                End If
                If Not lbProblemSolved Then
                  m_tree.Errors.Clear
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                        lbProblemSolved = True
                        tok = m_scanner.Scan(TokenType.KEYWD_POSSIBLE_VALUES_OF) ' Terminal Rule: KEYWD_POSSIBLE_VALUES_OF
            n = node.CreateNode(tok, tok.ToString() )
            node.Token.UpdateRange(tok)
            node.Nodes.Add(n)
            If m_scanner.StartPos >= Me.MaxDistance Then
              m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_POSSIBLE_VALUES_OF.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_POSSIBLE_VALUES_OF"))
            End If
            If tok.Type <> TokenType.KEYWD_POSSIBLE_VALUES_OF Then
              m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_POSSIBLE_VALUES_OF.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_POSSIBLE_VALUES_OF"))
              lbProblemSolved = False
              If liMaxRange >= Me.MaxDistance Then
                Me.MaxDistance = m_scanner.StartPos
                max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_POSSIBLE_VALUES_OF.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_POSSIBLE_VALUES_OF"))
                max_tree = m_tree.clone
              End If
              Return False

            Else
            If m_scanner.StartPos >= Me.MaxDistance Then
                m_tree.Optionals.Clear
            End If
            End If


                End If
                If Not lbProblemSolved Then
                  m_tree.Errors.Clear
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                        lbProblemSolved = True
                        tok = m_scanner.Scan(TokenType.KEYWD_BY_DEFINITION) ' Terminal Rule: KEYWD_BY_DEFINITION
            n = node.CreateNode(tok, tok.ToString() )
            node.Token.UpdateRange(tok)
            node.Nodes.Add(n)
            If m_scanner.StartPos >= Me.MaxDistance Then
              m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_BY_DEFINITION.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_BY_DEFINITION"))
            End If
            If tok.Type <> TokenType.KEYWD_BY_DEFINITION Then
              m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_BY_DEFINITION.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_BY_DEFINITION"))
              lbProblemSolved = False
              If liMaxRange >= Me.MaxDistance Then
                Me.MaxDistance = m_scanner.StartPos
                max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_BY_DEFINITION.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_BY_DEFINITION"))
                max_tree = m_tree.clone
              End If
              Return False

            Else
            If m_scanner.StartPos >= Me.MaxDistance Then
                m_tree.Optionals.Clear
            End If
            End If


                End If
                If Not lbProblemSolved Then
                  m_tree.Errors.Clear
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                        lbProblemSolved = True
                        tok = m_scanner.Scan(TokenType.KEYWD_AT_LEAST) ' Terminal Rule: KEYWD_AT_LEAST
            n = node.CreateNode(tok, tok.ToString() )
            node.Token.UpdateRange(tok)
            node.Nodes.Add(n)
            If m_scanner.StartPos >= Me.MaxDistance Then
              m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_AT_LEAST.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_AT_LEAST"))
            End If
            If tok.Type <> TokenType.KEYWD_AT_LEAST Then
              m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_AT_LEAST.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_AT_LEAST"))
              lbProblemSolved = False
              If liMaxRange >= Me.MaxDistance Then
                Me.MaxDistance = m_scanner.StartPos
                max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_AT_LEAST.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_AT_LEAST"))
                max_tree = m_tree.clone
              End If
              Return False

            Else
            If m_scanner.StartPos >= Me.MaxDistance Then
                m_tree.Optionals.Clear
            End If
            End If


                End If
                If Not lbProblemSolved Then
                  m_tree.Errors.Clear
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                        lbProblemSolved = True
                        tok = m_scanner.Scan(TokenType.KEYWD_AT_MOST) ' Terminal Rule: KEYWD_AT_MOST
            n = node.CreateNode(tok, tok.ToString() )
            node.Token.UpdateRange(tok)
            node.Nodes.Add(n)
            If m_scanner.StartPos >= Me.MaxDistance Then
              m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_AT_MOST.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_AT_MOST"))
            End If
            If tok.Type <> TokenType.KEYWD_AT_MOST Then
              m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_AT_MOST.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_AT_MOST"))
              lbProblemSolved = False
              If liMaxRange >= Me.MaxDistance Then
                Me.MaxDistance = m_scanner.StartPos
                max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_AT_MOST.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_AT_MOST"))
                max_tree = m_tree.clone
              End If
              Return False

            Else
            If m_scanner.StartPos >= Me.MaxDistance Then
                m_tree.Optionals.Clear
            End If
            End If


                End If
                If Not lbProblemSolved Then
                  m_tree.Errors.Clear
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
            lbProblemSolved =                 ParseFUNCTION_CALL(parent.Nodes(parent.Nodes.Count -1)) ' NonTerminal Rule: FUNCTION_CALL
                If m_tree.Errors.Count > 0 Then
                  If m_scanner.EndPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  lbProblemSolved = False
                Else If m_scanner.EndPos = Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.EndPos
                    max_tree = m_tree.clone
                End If

                End If
                If Not lbProblemSolved Then
                  m_tree.Errors.Clear
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
            lbProblemSolved =                 ParsePARENTHESISED_DERIVATION_EXPRESSION(parent.Nodes(parent.Nodes.Count -1)) ' NonTerminal Rule: PARENTHESISED_DERIVATION_EXPRESSION
                If m_tree.Errors.Count > 0 Then
                  If m_scanner.EndPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  lbProblemSolved = False
                Else If m_scanner.EndPos = Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.EndPos
                    max_tree = m_tree.clone
                End If

                End If
                If Not lbProblemSolved Then
                  m_tree.Errors.Clear
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                        lbProblemSolved = True
                        tok = m_scanner.Scan(TokenType.COMPARISON_OPERATOR) ' Terminal Rule: COMPARISON_OPERATOR
            n = node.CreateNode(tok, tok.ToString() )
            node.Token.UpdateRange(tok)
            node.Nodes.Add(n)
            If m_scanner.StartPos >= Me.MaxDistance Then
              m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.COMPARISON_OPERATOR.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "COMPARISON_OPERATOR"))
            End If
            If tok.Type <> TokenType.COMPARISON_OPERATOR Then
              m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.COMPARISON_OPERATOR.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "COMPARISON_OPERATOR"))
              lbProblemSolved = False
              If liMaxRange >= Me.MaxDistance Then
                Me.MaxDistance = m_scanner.StartPos
                max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.COMPARISON_OPERATOR.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "COMPARISON_OPERATOR"))
                max_tree = m_tree.clone
              End If
              Return False

            Else
            If m_scanner.StartPos >= Me.MaxDistance Then
                m_tree.Optionals.Clear
            End If
            End If


                End If
                If Not lbProblemSolved Then
                  m_tree.Errors.Clear
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                        lbProblemSolved = True
                        tok = m_scanner.Scan(TokenType.ARITHMETIC_OPERATOR) ' Terminal Rule: ARITHMETIC_OPERATOR
            n = node.CreateNode(tok, tok.ToString() )
            node.Token.UpdateRange(tok)
            node.Nodes.Add(n)
            If m_scanner.StartPos >= Me.MaxDistance Then
              m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.ARITHMETIC_OPERATOR.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "ARITHMETIC_OPERATOR"))
            End If
            If tok.Type <> TokenType.ARITHMETIC_OPERATOR Then
              m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.ARITHMETIC_OPERATOR.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "ARITHMETIC_OPERATOR"))
              lbProblemSolved = False
              If liMaxRange >= Me.MaxDistance Then
                Me.MaxDistance = m_scanner.StartPos
                max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.ARITHMETIC_OPERATOR.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "ARITHMETIC_OPERATOR"))
                max_tree = m_tree.clone
              End If
              Return False

            Else
            If m_scanner.StartPos >= Me.MaxDistance Then
                m_tree.Optionals.Clear
            End If
            End If


                End If
                If Not lbProblemSolved Then
                  m_tree.Errors.Clear
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                        lbProblemSolved = True
                        tok = m_scanner.Scan(TokenType.NUMBER_LITERAL) ' Terminal Rule: NUMBER_LITERAL
            n = node.CreateNode(tok, tok.ToString() )
            node.Token.UpdateRange(tok)
            node.Nodes.Add(n)
            If m_scanner.StartPos >= Me.MaxDistance Then
              m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.NUMBER_LITERAL.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "NUMBER_LITERAL"))
            End If
            If tok.Type <> TokenType.NUMBER_LITERAL Then
              m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.NUMBER_LITERAL.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "NUMBER_LITERAL"))
              lbProblemSolved = False
              If liMaxRange >= Me.MaxDistance Then
                Me.MaxDistance = m_scanner.StartPos
                max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.NUMBER_LITERAL.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "NUMBER_LITERAL"))
                max_tree = m_tree.clone
              End If
              Return False

            Else
            If m_scanner.StartPos >= Me.MaxDistance Then
                m_tree.Optionals.Clear
            End If
            End If


                End If
                If Not lbProblemSolved Then
                  m_tree.Errors.Clear
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                        lbProblemSolved = True
                        tok = m_scanner.Scan(TokenType.TEXT_LITERAL) ' Terminal Rule: TEXT_LITERAL
            n = node.CreateNode(tok, tok.ToString() )
            node.Token.UpdateRange(tok)
            node.Nodes.Add(n)
            If m_scanner.StartPos >= Me.MaxDistance Then
              m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.TEXT_LITERAL.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "TEXT_LITERAL"))
            End If
            If tok.Type <> TokenType.TEXT_LITERAL Then
              m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.TEXT_LITERAL.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "TEXT_LITERAL"))
              lbProblemSolved = False
              If liMaxRange >= Me.MaxDistance Then
                Me.MaxDistance = m_scanner.StartPos
                max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.TEXT_LITERAL.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "TEXT_LITERAL"))
                max_tree = m_tree.clone
              End If
              Return False

            Else
            If m_scanner.StartPos >= Me.MaxDistance Then
                m_tree.Optionals.Clear
            End If
            End If


                End If
                If Not lbProblemSolved Then
                  m_tree.Errors.Clear
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                        lbProblemSolved = True
                        tok = m_scanner.Scan(TokenType.COMMA_SEPARATOR) ' Terminal Rule: COMMA_SEPARATOR
            n = node.CreateNode(tok, tok.ToString() )
            node.Token.UpdateRange(tok)
            node.Nodes.Add(n)
            If m_scanner.StartPos >= Me.MaxDistance Then
              m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.COMMA_SEPARATOR.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "COMMA_SEPARATOR"))
            End If
            If tok.Type <> TokenType.COMMA_SEPARATOR Then
              m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.COMMA_SEPARATOR.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "COMMA_SEPARATOR"))
              lbProblemSolved = False
              If liMaxRange >= Me.MaxDistance Then
                Me.MaxDistance = m_scanner.StartPos
                max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.COMMA_SEPARATOR.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "COMMA_SEPARATOR"))
                max_tree = m_tree.clone
              End If
              Return False

            Else
            If m_scanner.StartPos >= Me.MaxDistance Then
                m_tree.Optionals.Clear
            End If
            End If


                End If
                If Not lbProblemSolved Then
                  m_tree.Errors.Clear
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
            lbProblemSolved =                 ParseMODEL_ELEMENT_REFERENCE(parent.Nodes(parent.Nodes.Count -1)) ' NonTerminal Rule: MODEL_ELEMENT_REFERENCE
                If m_tree.Errors.Count > 0 Then
                  If m_scanner.EndPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  lbProblemSolved = False
                Else If m_scanner.EndPos = Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.EndPos
                    max_tree = m_tree.clone
                End If

                End If
                If Not lbProblemSolved Then
                  m_tree.Errors.Clear
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                        lbProblemSolved = True
                        tok = m_scanner.Scan(TokenType.PREDICATE_WORD) ' Terminal Rule: PREDICATE_WORD
            n = node.CreateNode(tok, tok.ToString() )
            node.Token.UpdateRange(tok)
            node.Nodes.Add(n)
            If m_scanner.StartPos >= Me.MaxDistance Then
              m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.PREDICATE_WORD.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "PREDICATE_WORD"))
            End If
            If tok.Type <> TokenType.PREDICATE_WORD Then
              m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.PREDICATE_WORD.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "PREDICATE_WORD"))
              lbProblemSolved = False
              If liMaxRange >= Me.MaxDistance Then
                Me.MaxDistance = m_scanner.StartPos
                max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.PREDICATE_WORD.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "PREDICATE_WORD"))
                max_tree = m_tree.clone
              End If
              Return False

            Else
            If m_scanner.StartPos >= Me.MaxDistance Then
                m_tree.Optionals.Clear
            End If
            End If


                End If
                End If
             If (m_tree.Errors.Count > 0) Or Not lbProblemSolved Then
                 parent.Nodes.Remove(node)
                 Return False
             End If

            If m_scanner.Input.Length > (parent.Token.EndPos + 1) Then
            End If
            Finally
                If lbProblemSolved Then
                    parent.Token.UpdateRange(node.Token)
                    Me.MaxDistance = node.Token.EndPos
                    If m_scanner.EndPos >= Me.MaxDistance Then
                       If m_tree.MaxDistance > max_tree.MaxDistance Then
                          Me.MaxDistance = m_scanner.StartPos
                          max_tree = m_tree.clone
                       End If
                    End If
                Else
                   m_scanner.StartPos = liOriginalRange
                   parent.Nodes.Remove(node)
                End If
            End Try
            Return lbProblemSolved
        End Function ' NonTerminalSymbol: FACT_TYPE_PATH_COMPONENT

        Private Function ParseFUNCTION_CALL(ByVal parent As ParseNode) As Boolean ' NonTerminalSymbol: FUNCTION_CALL
            Dim tok As Token
            Dim n As ParseNode
            Dim node As ParseNode = parent.CreateNode(m_scanner.GetToken(TokenType.FUNCTION_CALL), "FUNCTION_CALL")
            Dim lbProblemSolved As Boolean = True

            Dim liOriginalRange as Integer = m_scanner.StartPos
            Dim liMaxRange as Integer = liOriginalRange
            parent.Nodes.Add(node)

            Try

             ' Concat Rule
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.FUNCTION_NAME) ' Terminal Rule: FUNCTION_NAME
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.FUNCTION_NAME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "FUNCTION_NAME"))
                    End If
                    If tok.Type <> TokenType.FUNCTION_NAME Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.FUNCTION_NAME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "FUNCTION_NAME"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.FUNCTION_NAME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "FUNCTION_NAME"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If

             ' Concat Rule
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.OPEN_PARENTHESIS) ' Terminal Rule: OPEN_PARENTHESIS
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.OPEN_PARENTHESIS.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "OPEN_PARENTHESIS"))
                    End If
                    If tok.Type <> TokenType.OPEN_PARENTHESIS Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.OPEN_PARENTHESIS.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "OPEN_PARENTHESIS"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.OPEN_PARENTHESIS.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "OPEN_PARENTHESIS"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If

             ' Concat Rule
                                tok = m_scanner.LookAhead({TokenType.KEYWD_NEGATED, TokenType.KEYWD_SOME, TokenType.KEYWD_EACH, TokenType.KEYWD_NO, TokenType.KEYWD_THAT, TokenType.KEYWD_POSSIBLE_VALUES_OF, TokenType.KEYWD_BY_DEFINITION, TokenType.KEYWD_AT_LEAST, TokenType.KEYWD_AT_MOST, TokenType.FUNCTION_NAME, TokenType.OPEN_PARENTHESIS, TokenType.COMPARISON_OPERATOR, TokenType.ARITHMETIC_OPERATOR, TokenType.NUMBER_LITERAL, TokenType.TEXT_LITERAL, TokenType.COMMA_SEPARATOR, TokenType.MODEL_ELEMENT_NAME, TokenType.PREDICATE_WORD}) ' Option Rule
                    If m_scanner.EndPos >= Me.MaxDistance Then
                                            max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_NEGATED.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_NEGATED"))
                                            max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_SOME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_SOME"))
                                            max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_EACH.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_EACH"))
                                            max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_NO.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_NO"))
                                            max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_THAT.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_THAT"))
                                            max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_POSSIBLE_VALUES_OF.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_POSSIBLE_VALUES_OF"))
                                            max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_BY_DEFINITION.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_BY_DEFINITION"))
                                            max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_AT_LEAST.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_AT_LEAST"))
                                            max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_AT_MOST.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_AT_MOST"))
                                            max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.FUNCTION_NAME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "FUNCTION_NAME"))
                                            max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.OPEN_PARENTHESIS.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "OPEN_PARENTHESIS"))
                                            max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.COMPARISON_OPERATOR.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "COMPARISON_OPERATOR"))
                                            max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.ARITHMETIC_OPERATOR.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "ARITHMETIC_OPERATOR"))
                                            max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.NUMBER_LITERAL.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "NUMBER_LITERAL"))
                                            max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.TEXT_LITERAL.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "TEXT_LITERAL"))
                                            max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.COMMA_SEPARATOR.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "COMMA_SEPARATOR"))
                                            max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.MODEL_ELEMENT_NAME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "MODEL_ELEMENT_NAME"))
                                            max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.PREDICATE_WORD.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "PREDICATE_WORD"))
                    End If
                    If tok.Type = TokenType.KEYWD_NEGATED Or tok.Type = TokenType.KEYWD_SOME Or tok.Type = TokenType.KEYWD_EACH Or tok.Type = TokenType.KEYWD_NO Or tok.Type = TokenType.KEYWD_THAT Or tok.Type = TokenType.KEYWD_POSSIBLE_VALUES_OF Or tok.Type = TokenType.KEYWD_BY_DEFINITION Or tok.Type = TokenType.KEYWD_AT_LEAST Or tok.Type = TokenType.KEYWD_AT_MOST Or tok.Type = TokenType.FUNCTION_NAME Or tok.Type = TokenType.OPEN_PARENTHESIS Or tok.Type = TokenType.COMPARISON_OPERATOR Or tok.Type = TokenType.ARITHMETIC_OPERATOR Or tok.Type = TokenType.NUMBER_LITERAL Or tok.Type = TokenType.TEXT_LITERAL Or tok.Type = TokenType.COMMA_SEPARATOR Or tok.Type = TokenType.MODEL_ELEMENT_NAME Or tok.Type = TokenType.PREDICATE_WORD Then
lbProblemSolved =                         ParseFUNCTION_ARGUMENT_LIST(node) ' NonTerminal Rule: FUNCTION_ARGUMENT_LIST
                        If m_tree.Errors.Count > 0 Then
                          If m_scanner.EndPos > Me.MaxDistance Then
                            Me.MaxDistance = m_scanner.StartPos
                            max_tree = m_tree.clone
                          End If
                          lbProblemSolved = False
                        End If

                    Else
                                            m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWD_NEGATED.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWD_NEGATED"))
                    End If
                    If m_tree.Errors.Count > 0 Then
                      Return False
                    End If

               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If

             ' Concat Rule
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.CLOSE_PARENTHESIS) ' Terminal Rule: CLOSE_PARENTHESIS
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.CLOSE_PARENTHESIS.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "CLOSE_PARENTHESIS"))
                    End If
                    If tok.Type <> TokenType.CLOSE_PARENTHESIS Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.CLOSE_PARENTHESIS.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "CLOSE_PARENTHESIS"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.CLOSE_PARENTHESIS.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "CLOSE_PARENTHESIS"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If
               If m_tree.Errors.Count > 0 Then
                  Return False
               End If

            If m_scanner.Input.Length > (parent.Token.EndPos + 1) Then
            End If
            Finally
                If lbProblemSolved Then
                    parent.Token.UpdateRange(node.Token)
                    Me.MaxDistance = node.Token.EndPos
                    If m_scanner.EndPos >= Me.MaxDistance Then
                       If m_tree.MaxDistance > max_tree.MaxDistance Then
                          Me.MaxDistance = m_scanner.StartPos
                          max_tree = m_tree.clone
                       End If
                    End If
                Else
                   m_scanner.StartPos = liOriginalRange
                   parent.Nodes.Remove(node)
                End If
            End Try
            Return lbProblemSolved
        End Function ' NonTerminalSymbol: FUNCTION_CALL

        Private Function ParseFUNCTION_ARGUMENT_LIST(ByVal parent As ParseNode) As Boolean ' NonTerminalSymbol: FUNCTION_ARGUMENT_LIST
            Dim tok As Token
            Dim n As ParseNode
            Dim node As ParseNode = parent.CreateNode(m_scanner.GetToken(TokenType.FUNCTION_ARGUMENT_LIST), "FUNCTION_ARGUMENT_LIST")
            Dim lbProblemSolved As Boolean = True

            Dim liOriginalRange as Integer = m_scanner.StartPos
            Dim liMaxRange as Integer = liOriginalRange
            parent.Nodes.Add(node)

            Try

             ' Concat Rule
            lbProblemSolved =                     ParseDERIVATION_EXPRESSION(node) ' NonTerminal Rule: DERIVATION_EXPRESSION
                    If m_tree.Errors.Count > 0 Then
                      If m_scanner.EndPos > Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree = m_tree.clone
                      End If
                      lbProblemSolved = False
                    End If

               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If

             ' Concat Rule
                                tok = m_scanner.LookAhead(TokenType.COMMA_SEPARATOR) ' ZeroOrMore Rule
                    While tok.Type = TokenType.COMMA_SEPARATOR
                    m_tree.Errors.Clear

                         ' Concat Rule
                                                        lbProblemSolved = True
                                                                tok = m_scanner.Scan(TokenType.COMMA_SEPARATOR) ' Terminal Rule: COMMA_SEPARATOR
                                n = node.CreateNode(tok, tok.ToString() )
                                node.Token.UpdateRange(tok)
                                node.Nodes.Add(n)
                                If m_scanner.StartPos >= Me.MaxDistance Then
                                  m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.COMMA_SEPARATOR.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "COMMA_SEPARATOR"))
                                End If
                                If tok.Type <> TokenType.COMMA_SEPARATOR Then
                                  m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.COMMA_SEPARATOR.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "COMMA_SEPARATOR"))
                                  lbProblemSolved = False
                                  If liMaxRange >= Me.MaxDistance Then
                                    Me.MaxDistance = m_scanner.StartPos
                                    max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.COMMA_SEPARATOR.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "COMMA_SEPARATOR"))
                                    max_tree = m_tree.clone
                                  End If
                                  Return False

                                Else
                                If m_scanner.StartPos >= Me.MaxDistance Then
                                    m_tree.Optionals.Clear
                                End If
                                End If


                           If Not lbProblemSolved Then
                              If m_scanner.StartPos > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                              m_scanner.StartPos = liMaxRange
                              Return False
                           Else
                              liMaxRange = m_scanner.EndPos
                              If liMaxRange > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                           End If

                         ' Concat Rule
                        lbProblemSolved =                                 ParseDERIVATION_EXPRESSION(node) ' NonTerminal Rule: DERIVATION_EXPRESSION
                                If m_tree.Errors.Count > 0 Then
                                  If m_scanner.EndPos > Me.MaxDistance Then
                                    Me.MaxDistance = m_scanner.StartPos
                                    max_tree = m_tree.clone
                                  End If
                                  lbProblemSolved = False
                                End If

                           If Not lbProblemSolved Then
                              If m_scanner.StartPos > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                              m_scanner.StartPos = liMaxRange
                              Return False
                           Else
                              liMaxRange = m_scanner.EndPos
                              If liMaxRange > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                           End If
                           If m_tree.Errors.Count > 0 Then
                              Return False
                           End If
                    tok = m_scanner.LookAhead(TokenType.COMMA_SEPARATOR) ' ZeroOrMore Rule
                    If Not lbProblemSolved Then Exit While
                    End While
            If m_tree.Errors.Count > 0 Then
                                Return False
            End If

               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If
               If m_tree.Errors.Count > 0 Then
                  Return False
               End If

            If m_scanner.Input.Length > (parent.Token.EndPos + 1) Then
            End If
            Finally
                If lbProblemSolved Then
                    parent.Token.UpdateRange(node.Token)
                    Me.MaxDistance = node.Token.EndPos
                    If m_scanner.EndPos >= Me.MaxDistance Then
                       If m_tree.MaxDistance > max_tree.MaxDistance Then
                          Me.MaxDistance = m_scanner.StartPos
                          max_tree = m_tree.clone
                       End If
                    End If
                Else
                   m_scanner.StartPos = liOriginalRange
                   parent.Nodes.Remove(node)
                End If
            End Try
            Return lbProblemSolved
        End Function ' NonTerminalSymbol: FUNCTION_ARGUMENT_LIST

        Private Function ParsePARENTHESISED_DERIVATION_EXPRESSION(ByVal parent As ParseNode) As Boolean ' NonTerminalSymbol: PARENTHESISED_DERIVATION_EXPRESSION
            Dim tok As Token
            Dim n As ParseNode
            Dim node As ParseNode = parent.CreateNode(m_scanner.GetToken(TokenType.PARENTHESISED_DERIVATION_EXPRESSION), "PARENTHESISED_DERIVATION_EXPRESSION")
            Dim lbProblemSolved As Boolean = True

            Dim liOriginalRange as Integer = m_scanner.StartPos
            Dim liMaxRange as Integer = liOriginalRange
            parent.Nodes.Add(node)

            Try

             ' Concat Rule
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.OPEN_PARENTHESIS) ' Terminal Rule: OPEN_PARENTHESIS
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.OPEN_PARENTHESIS.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "OPEN_PARENTHESIS"))
                    End If
                    If tok.Type <> TokenType.OPEN_PARENTHESIS Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.OPEN_PARENTHESIS.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "OPEN_PARENTHESIS"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.OPEN_PARENTHESIS.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "OPEN_PARENTHESIS"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If

             ' Concat Rule
            lbProblemSolved =                     ParseDERIVATION_EXPRESSION(node) ' NonTerminal Rule: DERIVATION_EXPRESSION
                    If m_tree.Errors.Count > 0 Then
                      If m_scanner.EndPos > Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree = m_tree.clone
                      End If
                      lbProblemSolved = False
                    End If

               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If

             ' Concat Rule
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.CLOSE_PARENTHESIS) ' Terminal Rule: CLOSE_PARENTHESIS
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.CLOSE_PARENTHESIS.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "CLOSE_PARENTHESIS"))
                    End If
                    If tok.Type <> TokenType.CLOSE_PARENTHESIS Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.CLOSE_PARENTHESIS.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "CLOSE_PARENTHESIS"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.CLOSE_PARENTHESIS.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "CLOSE_PARENTHESIS"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If
               If m_tree.Errors.Count > 0 Then
                  Return False
               End If

            If m_scanner.Input.Length > (parent.Token.EndPos + 1) Then
            End If
            Finally
                If lbProblemSolved Then
                    parent.Token.UpdateRange(node.Token)
                    Me.MaxDistance = node.Token.EndPos
                    If m_scanner.EndPos >= Me.MaxDistance Then
                       If m_tree.MaxDistance > max_tree.MaxDistance Then
                          Me.MaxDistance = m_scanner.StartPos
                          max_tree = m_tree.clone
                       End If
                    End If
                Else
                   m_scanner.StartPos = liOriginalRange
                   parent.Nodes.Remove(node)
                End If
            End Try
            Return lbProblemSolved
        End Function ' NonTerminalSymbol: PARENTHESISED_DERIVATION_EXPRESSION

        Private Function ParseStart(ByVal parent As ParseNode) As Boolean ' NonTerminalSymbol: Start
            Dim tok As Token
            Dim n As ParseNode
            Dim node As ParseNode = parent.CreateNode(m_scanner.GetToken(TokenType.Start), "Start")
            Dim lbProblemSolved As Boolean = True

            Dim liOriginalRange as Integer = m_scanner.StartPos
            Dim liMaxRange as Integer = liOriginalRange
            parent.Nodes.Add(node)

            Try

             ' Concat Rule
            lbProblemSolved =                     ParseDERIVATION(node) ' NonTerminal Rule: DERIVATION
                    If m_tree.Errors.Count > 0 Then
                      If m_scanner.EndPos > Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree = m_tree.clone
                      End If
                      lbProblemSolved = False
                    End If

               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If

             ' Concat Rule
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.EOF) ' Terminal Rule: EOF
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.EOF.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "EOF"))
                    End If
                    If tok.Type <> TokenType.EOF Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.EOF.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "EOF"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.EOF.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "EOF"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If
               If m_tree.Errors.Count > 0 Then
                  Return False
               End If

            If m_scanner.Input.Length > (parent.Token.EndPos + 1) Then
            End If
            Finally
                If lbProblemSolved Then
                    parent.Token.UpdateRange(node.Token)
                    Me.MaxDistance = node.Token.EndPos
                    If m_scanner.EndPos >= Me.MaxDistance Then
                       If m_tree.MaxDistance > max_tree.MaxDistance Then
                          Me.MaxDistance = m_scanner.StartPos
                          max_tree = m_tree.clone
                       End If
                    End If
                Else
                   m_scanner.StartPos = liOriginalRange
                   parent.Nodes.Remove(node)
                End If
            End Try
            Return lbProblemSolved
        End Function ' NonTerminalSymbol: Start



    End Class

#End Region
End Namespace

