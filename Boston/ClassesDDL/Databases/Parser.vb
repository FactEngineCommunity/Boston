' Generated by TinyPG v1.3 available at www.codeproject.com

Imports System
Imports System.Collections.Generic


Namespace FEQL
#Region "Parser"

    Partial Public Class Parser 
        Private m_scanner As Scanner
        Private m_tree As ParseTree
        Private max_tree As ParseTree
        Public MaxDistance As Integer 'The maximum distance the parser got within the input text.

        Public Sub New(ByVal scanner As Scanner)
            m_scanner = scanner
        End Sub


    Public Function Parse(ByVal input As String) As ParseTree
            m_tree = New ParseTree()
            'Return Parse(input, m_tree)  '20210810-VM-Was this, changed to max_tree below
            max_tree = New ParseTree 'Added, as above.
            Return Parse(input, max_tree) 'Added, as above.
        End Function

        Public Function Parse(ByVal input As String, ByVal tree As ParseTree) As ParseTree
            m_scanner.Init(input)
            MaxDistance = 0
            m_tree = tree
            ParseStart(m_tree)
            m_tree.Skipped = m_scanner.Skipped
            Return Me.max_tree 'm_tree '20210810-VM-Added max_tree, commented out m_tree
        End Function

        Private Function Parseddl(ByVal parent As ParseNode) As Boolean ' NonTerminalSymbol: ddl
            Dim tok As Token
            Dim n As ParseNode
            Dim node As ParseNode = parent.CreateNode(m_scanner.GetToken(TokenType.ddl), "ddl")
            Dim lbProblemSolved As Boolean = True

            Dim liOriginalRange as Integer = m_scanner.StartPos
            Dim liMaxRange as Integer = liOriginalRange
            parent.Nodes.Add(node)

            Try
            tok = m_scanner.LookAhead(TokenType.CREATE) ' ZeroOrMore Rule
            While tok.Type = TokenType.CREATE
            m_tree.Errors.Clear

                 ' Concat Rule
                lbProblemSolved =                         ParsecreateTableStatement(node) ' NonTerminal Rule: createTableStatement
                        If m_tree.Errors.Count > 0 Then
                          If m_scanner.EndPos > Me.MaxDistance Then
                            Me.MaxDistance = m_scanner.StartPos
                            max_tree = m_tree.clone
                          End If
                          lbProblemSolved = False
                        End If

                   If Not lbProblemSolved Then
                      If m_scanner.StartPos > Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree = m_tree.clone
                      End If
                      m_scanner.StartPos = liMaxRange
                      Return False
                   Else
                      liMaxRange = m_scanner.EndPos
                      If liMaxRange > Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree = m_tree.clone
                      End If
                   End If

                 ' Concat Rule
                                        lbProblemSolved = True
                                                tok = m_scanner.Scan(TokenType.SEMICOLON) ' Terminal Rule: SEMICOLON
                        n = node.CreateNode(tok, tok.ToString() )
                        node.Token.UpdateRange(tok)
                        node.Nodes.Add(n)
                        If m_scanner.StartPos >= Me.MaxDistance Then
                          m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.SEMICOLON.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "SEMICOLON"))
                        End If
                        If tok.Type <> TokenType.SEMICOLON Then
                          m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.SEMICOLON.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "SEMICOLON"))
                          lbProblemSolved = False
                          If liMaxRange >= Me.MaxDistance Then
                            Me.MaxDistance = m_scanner.StartPos
                            max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.SEMICOLON.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "SEMICOLON"))
                            max_tree = m_tree.clone
                          End If
                          Return False

                        Else
                        If m_scanner.StartPos >= Me.MaxDistance Then
                            m_tree.Optionals.Clear
                        End If
                        End If


                   If Not lbProblemSolved Then
                      If m_scanner.StartPos > Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree = m_tree.clone
                      End If
                      m_scanner.StartPos = liMaxRange
                      Return False
                   Else
                      liMaxRange = m_scanner.EndPos
                      If liMaxRange > Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree = m_tree.clone
                      End If
                   End If
                   If m_tree.Errors.Count > 0 Then
                      Return False
                   End If
            tok = m_scanner.LookAhead(TokenType.CREATE) ' ZeroOrMore Rule
            If Not lbProblemSolved Then Exit While
            End While
            If m_tree.Errors.Count > 0 Then
                        Return False
            End If

            If m_scanner.Input.Length > (parent.Token.EndPos + 1) Then
            End If
            Finally
                If lbProblemSolved Then
                    parent.Token.UpdateRange(node.Token)
                    Me.MaxDistance = node.Token.EndPos
                    If m_scanner.EndPos >= Me.MaxDistance Then
                       If m_tree.MaxDistance > max_tree.MaxDistance Then
                          Me.MaxDistance = m_scanner.StartPos
                          max_tree = m_tree.clone
                       End If
                    End If
                Else
                   m_scanner.StartPos = liOriginalRange
                   parent.Nodes.Remove(node)
                End If
            End Try
            Return lbProblemSolved
        End Function ' NonTerminalSymbol: ddl

        Private Function ParsecreateTableStatement(ByVal parent As ParseNode) As Boolean ' NonTerminalSymbol: createTableStatement
            Dim tok As Token
            Dim n As ParseNode
            Dim node As ParseNode = parent.CreateNode(m_scanner.GetToken(TokenType.createTableStatement), "createTableStatement")
            Dim lbProblemSolved As Boolean = True

            Dim liOriginalRange as Integer = m_scanner.StartPos
            Dim liMaxRange as Integer = liOriginalRange
            parent.Nodes.Add(node)

            Try

             ' Concat Rule
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.CREATE) ' Terminal Rule: CREATE
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.CREATE.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "CREATE"))
                    End If
                    If tok.Type <> TokenType.CREATE Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.CREATE.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "CREATE"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.CREATE.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "CREATE"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If

             ' Concat Rule
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.TABLE) ' Terminal Rule: TABLE
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.TABLE.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "TABLE"))
                    End If
                    If tok.Type <> TokenType.TABLE Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.TABLE.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "TABLE"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.TABLE.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "TABLE"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If

             ' Concat Rule
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.IDENTIFIER) ' Terminal Rule: IDENTIFIER
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.IDENTIFIER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                    End If
                    If tok.Type <> TokenType.IDENTIFIER Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.IDENTIFIER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.IDENTIFIER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If

             ' Concat Rule
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.LPAREN) ' Terminal Rule: LPAREN
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.LPAREN.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "LPAREN"))
                    End If
                    If tok.Type <> TokenType.LPAREN Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.LPAREN.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "LPAREN"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.LPAREN.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "LPAREN"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If

             ' Concat Rule
                                tok = m_scanner.LookAhead({TokenType.KEYWDCONSTRAINT, TokenType.REFERENCES, TokenType.IDENTIFIER}) ' Choice Rule
                    
                        m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWDCONSTRAINT.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWDCONSTRAINT"))
                        m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWDCONSTRAINT.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "REFERENCES"))
                        m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWDCONSTRAINT.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                    Select Case tok.Type
                     ' Choice Rule
                        Case TokenType.KEYWDCONSTRAINT
                    lbProblemSolved =                             ParseCONSTRAINTDEFINITION(node) ' NonTerminal Rule: CONSTRAINTDEFINITION
                            If m_tree.Errors.Count > 0 Then
                              If m_scanner.EndPos > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                              lbProblemSolved = False
                            End If

                        Case TokenType.REFERENCES
                    lbProblemSolved =                             ParseCONSTRAINTDEFINITION(node) ' NonTerminal Rule: CONSTRAINTDEFINITION
                            If m_tree.Errors.Count > 0 Then
                              If m_scanner.EndPos > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                              lbProblemSolved = False
                            End If

                        Case TokenType.IDENTIFIER
                    lbProblemSolved =                             ParseCOLUMNDEFINITION(node) ' NonTerminal Rule: COLUMNDEFINITION
                            If m_tree.Errors.Count > 0 Then
                              If m_scanner.EndPos > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                              lbProblemSolved = False
                            End If

                        Case Else
                        If m_tree.Errors.Count = 0 Then
                        lbProblemSolved = False
                        m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWDCONSTRAINT.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWDCONSTRAINT"))
                        m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWDCONSTRAINT.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "REFERENCES"))
                        m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWDCONSTRAINT.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                        End If
                            m_tree.Errors.Add(new ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found.", &H0002, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos))
                            Exit Select
                    End Select ' Choice Rule
                        If Not lbProblemSolved Then
                           m_tree.Errors.Clear
                        If Not lbProblemSolved Then
                          m_tree.Errors.Clear
                          If liMaxRange > Me.MaxDistance Then
                            Me.MaxDistance = m_scanner.StartPos
                            max_tree = m_tree.clone
                          End If
                    lbProblemSolved =                         ParseCOLUMNDEFINITION(parent.Nodes(parent.Nodes.Count -1)) ' NonTerminal Rule: COLUMNDEFINITION
                        If m_tree.Errors.Count > 0 Then
                          If m_scanner.EndPos > Me.MaxDistance Then
                            Me.MaxDistance = m_scanner.StartPos
                            max_tree = m_tree.clone
                          End If
                          lbProblemSolved = False
                        Else If m_scanner.EndPos = Me.MaxDistance Then
                            Me.MaxDistance = m_scanner.EndPos
                            max_tree = m_tree.clone
                        End If

                        End If
                        End If
                     If (m_tree.Errors.Count > 0) Or Not lbProblemSolved Then
                         parent.Nodes.Remove(node)
                         Return False
                     End If

               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If

             ' Concat Rule
                                tok = m_scanner.LookAhead(TokenType.COMMA) ' ZeroOrMore Rule
                    While tok.Type = TokenType.COMMA
                    m_tree.Errors.Clear

                         ' Concat Rule
                                                        lbProblemSolved = True
                                                                tok = m_scanner.Scan(TokenType.COMMA) ' Terminal Rule: COMMA
                                n = node.CreateNode(tok, tok.ToString() )
                                node.Token.UpdateRange(tok)
                                node.Nodes.Add(n)
                                If m_scanner.StartPos >= Me.MaxDistance Then
                                  m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.COMMA.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "COMMA"))
                                End If
                                If tok.Type <> TokenType.COMMA Then
                                  m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.COMMA.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "COMMA"))
                                  lbProblemSolved = False
                                  If liMaxRange >= Me.MaxDistance Then
                                    Me.MaxDistance = m_scanner.StartPos
                                    max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.COMMA.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "COMMA"))
                                    max_tree = m_tree.clone
                                  End If
                                  Return False

                                Else
                                If m_scanner.StartPos >= Me.MaxDistance Then
                                    m_tree.Optionals.Clear
                                End If
                                End If


                           If Not lbProblemSolved Then
                              If m_scanner.StartPos > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                              m_scanner.StartPos = liMaxRange
                              Return False
                           Else
                              liMaxRange = m_scanner.EndPos
                              If liMaxRange > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                           End If

                         ' Concat Rule
                                                        tok = m_scanner.LookAhead({TokenType.KEYWDCONSTRAINT, TokenType.REFERENCES, TokenType.IDENTIFIER}) ' Choice Rule
                                
                                    m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWDCONSTRAINT.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWDCONSTRAINT"))
                                    m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWDCONSTRAINT.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "REFERENCES"))
                                    m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWDCONSTRAINT.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                                Select Case tok.Type
                                 ' Choice Rule
                                    Case TokenType.KEYWDCONSTRAINT
                                lbProblemSolved =                                         ParseCONSTRAINTDEFINITION(node) ' NonTerminal Rule: CONSTRAINTDEFINITION
                                        If m_tree.Errors.Count > 0 Then
                                          If m_scanner.EndPos > Me.MaxDistance Then
                                            Me.MaxDistance = m_scanner.StartPos
                                            max_tree = m_tree.clone
                                          End If
                                          lbProblemSolved = False
                                        End If

                                    Case TokenType.REFERENCES
                                lbProblemSolved =                                         ParseCONSTRAINTDEFINITION(node) ' NonTerminal Rule: CONSTRAINTDEFINITION
                                        If m_tree.Errors.Count > 0 Then
                                          If m_scanner.EndPos > Me.MaxDistance Then
                                            Me.MaxDistance = m_scanner.StartPos
                                            max_tree = m_tree.clone
                                          End If
                                          lbProblemSolved = False
                                        End If

                                    Case TokenType.IDENTIFIER
                                lbProblemSolved =                                         ParseCOLUMNDEFINITION(node) ' NonTerminal Rule: COLUMNDEFINITION
                                        If m_tree.Errors.Count > 0 Then
                                          If m_scanner.EndPos > Me.MaxDistance Then
                                            Me.MaxDistance = m_scanner.StartPos
                                            max_tree = m_tree.clone
                                          End If
                                          lbProblemSolved = False
                                        End If

                                    Case Else
                                    If m_tree.Errors.Count = 0 Then
                                    lbProblemSolved = False
                                    m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWDCONSTRAINT.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWDCONSTRAINT"))
                                    m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWDCONSTRAINT.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "REFERENCES"))
                                    m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWDCONSTRAINT.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                                    End If
                                        m_tree.Errors.Add(new ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found.", &H0002, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos))
                                        Exit Select
                                End Select ' Choice Rule
                                    If Not lbProblemSolved Then
                                       m_tree.Errors.Clear
                                    If Not lbProblemSolved Then
                                      m_tree.Errors.Clear
                                      If liMaxRange > Me.MaxDistance Then
                                        Me.MaxDistance = m_scanner.StartPos
                                        max_tree = m_tree.clone
                                      End If
                                lbProblemSolved =                                     ParseCOLUMNDEFINITION(parent.Nodes(parent.Nodes.Count -1)) ' NonTerminal Rule: COLUMNDEFINITION
                                    If m_tree.Errors.Count > 0 Then
                                      If m_scanner.EndPos > Me.MaxDistance Then
                                        Me.MaxDistance = m_scanner.StartPos
                                        max_tree = m_tree.clone
                                      End If
                                      lbProblemSolved = False
                                    Else If m_scanner.EndPos = Me.MaxDistance Then
                                        Me.MaxDistance = m_scanner.EndPos
                                        max_tree = m_tree.clone
                                    End If

                                    End If
                                    End If
                                 If (m_tree.Errors.Count > 0) Or Not lbProblemSolved Then
                                     parent.Nodes.Remove(node)
                                     Return False
                                 End If

                           If Not lbProblemSolved Then
                              If m_scanner.StartPos > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                              m_scanner.StartPos = liMaxRange
                              Return False
                           Else
                              liMaxRange = m_scanner.EndPos
                              If liMaxRange > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                           End If
                           If m_tree.Errors.Count > 0 Then
                              Return False
                           End If
                    tok = m_scanner.LookAhead(TokenType.COMMA) ' ZeroOrMore Rule
                    If Not lbProblemSolved Then Exit While
                    End While
            If m_tree.Errors.Count > 0 Then
                                Return False
            End If

               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If

             ' Concat Rule
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.RPAREN) ' Terminal Rule: RPAREN
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.RPAREN.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "RPAREN"))
                    End If
                    If tok.Type <> TokenType.RPAREN Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.RPAREN.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "RPAREN"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.RPAREN.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "RPAREN"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If
               If m_tree.Errors.Count > 0 Then
                  Return False
               End If

            If m_scanner.Input.Length > (parent.Token.EndPos + 1) Then
            End If
            Finally
                If lbProblemSolved Then
                    parent.Token.UpdateRange(node.Token)
                    Me.MaxDistance = node.Token.EndPos
                    If m_scanner.EndPos >= Me.MaxDistance Then
                       If m_tree.MaxDistance > max_tree.MaxDistance Then
                          Me.MaxDistance = m_scanner.StartPos
                          max_tree = m_tree.clone
                       End If
                    End If
                Else
                   m_scanner.StartPos = liOriginalRange
                   parent.Nodes.Remove(node)
                End If
            End Try
            Return lbProblemSolved
        End Function ' NonTerminalSymbol: createTableStatement

        Private Function ParseCOLUMNDEFINITION(ByVal parent As ParseNode) As Boolean ' NonTerminalSymbol: COLUMNDEFINITION
            Dim tok As Token
            Dim n As ParseNode
            Dim node As ParseNode = parent.CreateNode(m_scanner.GetToken(TokenType.COLUMNDEFINITION), "COLUMNDEFINITION")
            Dim lbProblemSolved As Boolean = True

            Dim liOriginalRange as Integer = m_scanner.StartPos
            Dim liMaxRange as Integer = liOriginalRange
            parent.Nodes.Add(node)

            Try

             ' Concat Rule
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.IDENTIFIER) ' Terminal Rule: IDENTIFIER
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.IDENTIFIER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                    End If
                    If tok.Type <> TokenType.IDENTIFIER Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.IDENTIFIER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.IDENTIFIER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If

             ' Concat Rule
            lbProblemSolved =                     ParsedataType(node) ' NonTerminal Rule: dataType
                    If m_tree.Errors.Count > 0 Then
                      If m_scanner.EndPos > Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree = m_tree.clone
                      End If
                      lbProblemSolved = False
                    End If

               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If

             ' Concat Rule
                                tok = m_scanner.LookAhead({TokenType.REFERENCES}) ' Option Rule
                    If m_scanner.EndPos >= Me.MaxDistance Then
                                            max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.REFERENCES.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "REFERENCES"))
                    End If
                    If tok.Type = TokenType.REFERENCES Then

                         ' Concat Rule
                                                        lbProblemSolved = True
                                                                tok = m_scanner.Scan(TokenType.REFERENCES) ' Terminal Rule: REFERENCES
                                n = node.CreateNode(tok, tok.ToString() )
                                node.Token.UpdateRange(tok)
                                node.Nodes.Add(n)
                                If m_scanner.StartPos >= Me.MaxDistance Then
                                  m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.REFERENCES.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "REFERENCES"))
                                End If
                                If tok.Type <> TokenType.REFERENCES Then
                                  m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.REFERENCES.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "REFERENCES"))
                                  lbProblemSolved = False
                                  If liMaxRange >= Me.MaxDistance Then
                                    Me.MaxDistance = m_scanner.StartPos
                                    max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.REFERENCES.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "REFERENCES"))
                                    max_tree = m_tree.clone
                                  End If
                                  Return False

                                Else
                                If m_scanner.StartPos >= Me.MaxDistance Then
                                    m_tree.Optionals.Clear
                                End If
                                End If


                           If Not lbProblemSolved Then
                              If m_scanner.StartPos > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                              m_scanner.StartPos = liMaxRange
                              Return False
                           Else
                              liMaxRange = m_scanner.EndPos
                              If liMaxRange > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                           End If

                         ' Concat Rule
                                                        lbProblemSolved = True
                                                                tok = m_scanner.Scan(TokenType.IDENTIFIER) ' Terminal Rule: IDENTIFIER
                                n = node.CreateNode(tok, tok.ToString() )
                                node.Token.UpdateRange(tok)
                                node.Nodes.Add(n)
                                If m_scanner.StartPos >= Me.MaxDistance Then
                                  m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.IDENTIFIER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                                End If
                                If tok.Type <> TokenType.IDENTIFIER Then
                                  m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.IDENTIFIER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                                  lbProblemSolved = False
                                  If liMaxRange >= Me.MaxDistance Then
                                    Me.MaxDistance = m_scanner.StartPos
                                    max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.IDENTIFIER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                                    max_tree = m_tree.clone
                                  End If
                                  Return False

                                Else
                                If m_scanner.StartPos >= Me.MaxDistance Then
                                    m_tree.Optionals.Clear
                                End If
                                End If


                           If Not lbProblemSolved Then
                              If m_scanner.StartPos > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                              m_scanner.StartPos = liMaxRange
                              Return False
                           Else
                              liMaxRange = m_scanner.EndPos
                              If liMaxRange > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                           End If

                         ' Concat Rule
                                                        lbProblemSolved = True
                                                                tok = m_scanner.Scan(TokenType.LPAREN) ' Terminal Rule: LPAREN
                                n = node.CreateNode(tok, tok.ToString() )
                                node.Token.UpdateRange(tok)
                                node.Nodes.Add(n)
                                If m_scanner.StartPos >= Me.MaxDistance Then
                                  m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.LPAREN.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "LPAREN"))
                                End If
                                If tok.Type <> TokenType.LPAREN Then
                                  m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.LPAREN.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "LPAREN"))
                                  lbProblemSolved = False
                                  If liMaxRange >= Me.MaxDistance Then
                                    Me.MaxDistance = m_scanner.StartPos
                                    max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.LPAREN.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "LPAREN"))
                                    max_tree = m_tree.clone
                                  End If
                                  Return False

                                Else
                                If m_scanner.StartPos >= Me.MaxDistance Then
                                    m_tree.Optionals.Clear
                                End If
                                End If


                           If Not lbProblemSolved Then
                              If m_scanner.StartPos > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                              m_scanner.StartPos = liMaxRange
                              Return False
                           Else
                              liMaxRange = m_scanner.EndPos
                              If liMaxRange > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                           End If

                         ' Concat Rule
                                                        lbProblemSolved = True
                                                                tok = m_scanner.Scan(TokenType.IDENTIFIER) ' Terminal Rule: IDENTIFIER
                                n = node.CreateNode(tok, tok.ToString() )
                                node.Token.UpdateRange(tok)
                                node.Nodes.Add(n)
                                If m_scanner.StartPos >= Me.MaxDistance Then
                                  m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.IDENTIFIER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                                End If
                                If tok.Type <> TokenType.IDENTIFIER Then
                                  m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.IDENTIFIER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                                  lbProblemSolved = False
                                  If liMaxRange >= Me.MaxDistance Then
                                    Me.MaxDistance = m_scanner.StartPos
                                    max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.IDENTIFIER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                                    max_tree = m_tree.clone
                                  End If
                                  Return False

                                Else
                                If m_scanner.StartPos >= Me.MaxDistance Then
                                    m_tree.Optionals.Clear
                                End If
                                End If


                           If Not lbProblemSolved Then
                              If m_scanner.StartPos > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                              m_scanner.StartPos = liMaxRange
                              Return False
                           Else
                              liMaxRange = m_scanner.EndPos
                              If liMaxRange > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                           End If

                         ' Concat Rule
                                                        tok = m_scanner.LookAhead(TokenType.COMMA) ' ZeroOrMore Rule
                                While tok.Type = TokenType.COMMA
                                m_tree.Errors.Clear

                                     ' Concat Rule
                                                                                lbProblemSolved = True
                                                                                        tok = m_scanner.Scan(TokenType.COMMA) ' Terminal Rule: COMMA
                                            n = node.CreateNode(tok, tok.ToString() )
                                            node.Token.UpdateRange(tok)
                                            node.Nodes.Add(n)
                                            If m_scanner.StartPos >= Me.MaxDistance Then
                                              m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.COMMA.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "COMMA"))
                                            End If
                                            If tok.Type <> TokenType.COMMA Then
                                              m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.COMMA.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "COMMA"))
                                              lbProblemSolved = False
                                              If liMaxRange >= Me.MaxDistance Then
                                                Me.MaxDistance = m_scanner.StartPos
                                                max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.COMMA.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "COMMA"))
                                                max_tree = m_tree.clone
                                              End If
                                              Return False

                                            Else
                                            If m_scanner.StartPos >= Me.MaxDistance Then
                                                m_tree.Optionals.Clear
                                            End If
                                            End If


                                       If Not lbProblemSolved Then
                                          If m_scanner.StartPos > Me.MaxDistance Then
                                            Me.MaxDistance = m_scanner.StartPos
                                            max_tree = m_tree.clone
                                          End If
                                          m_scanner.StartPos = liMaxRange
                                          Return False
                                       Else
                                          liMaxRange = m_scanner.EndPos
                                          If liMaxRange > Me.MaxDistance Then
                                            Me.MaxDistance = m_scanner.StartPos
                                            max_tree = m_tree.clone
                                          End If
                                       End If

                                     ' Concat Rule
                                                                                lbProblemSolved = True
                                                                                        tok = m_scanner.Scan(TokenType.IDENTIFIER) ' Terminal Rule: IDENTIFIER
                                            n = node.CreateNode(tok, tok.ToString() )
                                            node.Token.UpdateRange(tok)
                                            node.Nodes.Add(n)
                                            If m_scanner.StartPos >= Me.MaxDistance Then
                                              m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.IDENTIFIER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                                            End If
                                            If tok.Type <> TokenType.IDENTIFIER Then
                                              m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.IDENTIFIER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                                              lbProblemSolved = False
                                              If liMaxRange >= Me.MaxDistance Then
                                                Me.MaxDistance = m_scanner.StartPos
                                                max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.IDENTIFIER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                                                max_tree = m_tree.clone
                                              End If
                                              Return False

                                            Else
                                            If m_scanner.StartPos >= Me.MaxDistance Then
                                                m_tree.Optionals.Clear
                                            End If
                                            End If


                                       If Not lbProblemSolved Then
                                          If m_scanner.StartPos > Me.MaxDistance Then
                                            Me.MaxDistance = m_scanner.StartPos
                                            max_tree = m_tree.clone
                                          End If
                                          m_scanner.StartPos = liMaxRange
                                          Return False
                                       Else
                                          liMaxRange = m_scanner.EndPos
                                          If liMaxRange > Me.MaxDistance Then
                                            Me.MaxDistance = m_scanner.StartPos
                                            max_tree = m_tree.clone
                                          End If
                                       End If
                                       If m_tree.Errors.Count > 0 Then
                                          Return False
                                       End If
                                tok = m_scanner.LookAhead(TokenType.COMMA) ' ZeroOrMore Rule
                                If Not lbProblemSolved Then Exit While
                                End While
            If m_tree.Errors.Count > 0 Then
                                            Return False
            End If

                           If Not lbProblemSolved Then
                              If m_scanner.StartPos > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                              m_scanner.StartPos = liMaxRange
                              Return False
                           Else
                              liMaxRange = m_scanner.EndPos
                              If liMaxRange > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                           End If

                         ' Concat Rule
                                                        lbProblemSolved = True
                                                                tok = m_scanner.Scan(TokenType.RPAREN) ' Terminal Rule: RPAREN
                                n = node.CreateNode(tok, tok.ToString() )
                                node.Token.UpdateRange(tok)
                                node.Nodes.Add(n)
                                If m_scanner.StartPos >= Me.MaxDistance Then
                                  m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.RPAREN.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "RPAREN"))
                                End If
                                If tok.Type <> TokenType.RPAREN Then
                                  m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.RPAREN.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "RPAREN"))
                                  lbProblemSolved = False
                                  If liMaxRange >= Me.MaxDistance Then
                                    Me.MaxDistance = m_scanner.StartPos
                                    max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.RPAREN.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "RPAREN"))
                                    max_tree = m_tree.clone
                                  End If
                                  Return False

                                Else
                                If m_scanner.StartPos >= Me.MaxDistance Then
                                    m_tree.Optionals.Clear
                                End If
                                End If


                           If Not lbProblemSolved Then
                              If m_scanner.StartPos > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                              m_scanner.StartPos = liMaxRange
                              Return False
                           Else
                              liMaxRange = m_scanner.EndPos
                              If liMaxRange > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                           End If
                           If m_tree.Errors.Count > 0 Then
                              Return False
                           End If

                    Else
                                            m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.REFERENCES.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "REFERENCES"))
                    End If
                    If m_tree.Errors.Count > 0 Then
                      Return False
                    End If

               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If
               If m_tree.Errors.Count > 0 Then
                  Return False
               End If

            If m_scanner.Input.Length > (parent.Token.EndPos + 1) Then
            End If
            Finally
                If lbProblemSolved Then
                    parent.Token.UpdateRange(node.Token)
                    Me.MaxDistance = node.Token.EndPos
                    If m_scanner.EndPos >= Me.MaxDistance Then
                       If m_tree.MaxDistance > max_tree.MaxDistance Then
                          Me.MaxDistance = m_scanner.StartPos
                          max_tree = m_tree.clone
                       End If
                    End If
                Else
                   m_scanner.StartPos = liOriginalRange
                   parent.Nodes.Remove(node)
                End If
            End Try
            Return lbProblemSolved
        End Function ' NonTerminalSymbol: COLUMNDEFINITION

        Private Function ParsedataType(ByVal parent As ParseNode) As Boolean ' NonTerminalSymbol: dataType
            Dim tok As Token
            Dim n As ParseNode
            Dim node As ParseNode = parent.CreateNode(m_scanner.GetToken(TokenType.dataType), "dataType")
            Dim lbProblemSolved As Boolean = True

            Dim liOriginalRange as Integer = m_scanner.StartPos
            Dim liMaxRange as Integer = liOriginalRange
            parent.Nodes.Add(node)

            Try
            tok = m_scanner.LookAhead({TokenType.KEYWDNUMBER, TokenType.TEXT, TokenType.DATETIME}) ' Choice Rule
            
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWDNUMBER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWDNUMBER"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWDNUMBER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "TEXT"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWDNUMBER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "DATETIME"))
            Select Case tok.Type
             ' Choice Rule
                Case TokenType.KEYWDNUMBER
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.KEYWDNUMBER) ' Terminal Rule: KEYWDNUMBER
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWDNUMBER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWDNUMBER"))
                    End If
                    If tok.Type <> TokenType.KEYWDNUMBER Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWDNUMBER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWDNUMBER"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWDNUMBER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWDNUMBER"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


                Case TokenType.TEXT
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.TEXT) ' Terminal Rule: TEXT
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.TEXT.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "TEXT"))
                    End If
                    If tok.Type <> TokenType.TEXT Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.TEXT.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "TEXT"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.TEXT.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "TEXT"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


                Case TokenType.DATETIME
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.DATETIME) ' Terminal Rule: DATETIME
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.DATETIME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "DATETIME"))
                    End If
                    If tok.Type <> TokenType.DATETIME Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.DATETIME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "DATETIME"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.DATETIME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "DATETIME"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


                Case Else
                If m_tree.Errors.Count = 0 Then
                lbProblemSolved = False
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWDNUMBER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWDNUMBER"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWDNUMBER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "TEXT"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWDNUMBER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "DATETIME"))
                End If
                    m_tree.Errors.Add(new ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found.", &H0002, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos))
                    Exit Select
            End Select ' Choice Rule
                If Not lbProblemSolved Then
                   m_tree.Errors.Clear
                If Not lbProblemSolved Then
                  m_tree.Errors.Clear
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                        lbProblemSolved = True
                        tok = m_scanner.Scan(TokenType.TEXT) ' Terminal Rule: TEXT
            n = node.CreateNode(tok, tok.ToString() )
            node.Token.UpdateRange(tok)
            node.Nodes.Add(n)
            If m_scanner.StartPos >= Me.MaxDistance Then
              m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.TEXT.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "TEXT"))
            End If
            If tok.Type <> TokenType.TEXT Then
              m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.TEXT.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "TEXT"))
              lbProblemSolved = False
              If liMaxRange >= Me.MaxDistance Then
                Me.MaxDistance = m_scanner.StartPos
                max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.TEXT.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "TEXT"))
                max_tree = m_tree.clone
              End If
              Return False

            Else
            If m_scanner.StartPos >= Me.MaxDistance Then
                m_tree.Optionals.Clear
            End If
            End If


                End If
                If Not lbProblemSolved Then
                  m_tree.Errors.Clear
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                        lbProblemSolved = True
                        tok = m_scanner.Scan(TokenType.DATETIME) ' Terminal Rule: DATETIME
            n = node.CreateNode(tok, tok.ToString() )
            node.Token.UpdateRange(tok)
            node.Nodes.Add(n)
            If m_scanner.StartPos >= Me.MaxDistance Then
              m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.DATETIME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "DATETIME"))
            End If
            If tok.Type <> TokenType.DATETIME Then
              m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.DATETIME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "DATETIME"))
              lbProblemSolved = False
              If liMaxRange >= Me.MaxDistance Then
                Me.MaxDistance = m_scanner.StartPos
                max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.DATETIME.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "DATETIME"))
                max_tree = m_tree.clone
              End If
              Return False

            Else
            If m_scanner.StartPos >= Me.MaxDistance Then
                m_tree.Optionals.Clear
            End If
            End If


                End If
                End If
             If (m_tree.Errors.Count > 0) Or Not lbProblemSolved Then
                 parent.Nodes.Remove(node)
                 Return False
             End If

            If m_scanner.Input.Length > (parent.Token.EndPos + 1) Then
            End If
            Finally
                If lbProblemSolved Then
                    parent.Token.UpdateRange(node.Token)
                    Me.MaxDistance = node.Token.EndPos
                    If m_scanner.EndPos >= Me.MaxDistance Then
                       If m_tree.MaxDistance > max_tree.MaxDistance Then
                          Me.MaxDistance = m_scanner.StartPos
                          max_tree = m_tree.clone
                       End If
                    End If
                Else
                   m_scanner.StartPos = liOriginalRange
                   parent.Nodes.Remove(node)
                End If
            End Try
            Return lbProblemSolved
        End Function ' NonTerminalSymbol: dataType

        Private Function ParseCONSTRAINTDEFINITION(ByVal parent As ParseNode) As Boolean ' NonTerminalSymbol: CONSTRAINTDEFINITION
            Dim tok As Token
            Dim n As ParseNode
            Dim node As ParseNode = parent.CreateNode(m_scanner.GetToken(TokenType.CONSTRAINTDEFINITION), "CONSTRAINTDEFINITION")
            Dim lbProblemSolved As Boolean = True

            Dim liOriginalRange as Integer = m_scanner.StartPos
            Dim liMaxRange as Integer = liOriginalRange
            parent.Nodes.Add(node)

            Try
            tok = m_scanner.LookAhead({TokenType.KEYWDCONSTRAINT, TokenType.REFERENCES}) ' Choice Rule
            
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWDCONSTRAINT.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWDCONSTRAINT"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWDCONSTRAINT.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "REFERENCES"))
            Select Case tok.Type
             ' Choice Rule
                Case TokenType.KEYWDCONSTRAINT
            
                     ' Concat Rule
                                                lbProblemSolved = True
                                                        tok = m_scanner.Scan(TokenType.KEYWDCONSTRAINT) ' Terminal Rule: KEYWDCONSTRAINT
                            n = node.CreateNode(tok, tok.ToString() )
                            node.Token.UpdateRange(tok)
                            node.Nodes.Add(n)
                            If m_scanner.StartPos >= Me.MaxDistance Then
                              m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWDCONSTRAINT.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWDCONSTRAINT"))
                            End If
                            If tok.Type <> TokenType.KEYWDCONSTRAINT Then
                              m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWDCONSTRAINT.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWDCONSTRAINT"))
                              lbProblemSolved = False
                              If liMaxRange >= Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWDCONSTRAINT.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWDCONSTRAINT"))
                                max_tree = m_tree.clone
                              End If
                              Return False

                            Else
                            If m_scanner.StartPos >= Me.MaxDistance Then
                                m_tree.Optionals.Clear
                            End If
                            End If


                       If Not lbProblemSolved Then
                          If m_scanner.StartPos > Me.MaxDistance Then
                            Me.MaxDistance = m_scanner.StartPos
                            max_tree = m_tree.clone
                          End If
                          m_scanner.StartPos = liMaxRange
                          Return False
                       Else
                          liMaxRange = m_scanner.EndPos
                          If liMaxRange > Me.MaxDistance Then
                            Me.MaxDistance = m_scanner.StartPos
                            max_tree = m_tree.clone
                          End If
                       End If

                     ' Concat Rule
                                                lbProblemSolved = True
                                                        tok = m_scanner.Scan(TokenType.IDENTIFIER) ' Terminal Rule: IDENTIFIER
                            n = node.CreateNode(tok, tok.ToString() )
                            node.Token.UpdateRange(tok)
                            node.Nodes.Add(n)
                            If m_scanner.StartPos >= Me.MaxDistance Then
                              m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.IDENTIFIER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                            End If
                            If tok.Type <> TokenType.IDENTIFIER Then
                              m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.IDENTIFIER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                              lbProblemSolved = False
                              If liMaxRange >= Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.IDENTIFIER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                                max_tree = m_tree.clone
                              End If
                              Return False

                            Else
                            If m_scanner.StartPos >= Me.MaxDistance Then
                                m_tree.Optionals.Clear
                            End If
                            End If


                       If Not lbProblemSolved Then
                          If m_scanner.StartPos > Me.MaxDistance Then
                            Me.MaxDistance = m_scanner.StartPos
                            max_tree = m_tree.clone
                          End If
                          m_scanner.StartPos = liMaxRange
                          Return False
                       Else
                          liMaxRange = m_scanner.EndPos
                          If liMaxRange > Me.MaxDistance Then
                            Me.MaxDistance = m_scanner.StartPos
                            max_tree = m_tree.clone
                          End If
                       End If

                     ' Concat Rule
                    lbProblemSolved =                             ParseprimaryKeyConstraint(node) ' NonTerminal Rule: primaryKeyConstraint
                            If m_tree.Errors.Count > 0 Then
                              If m_scanner.EndPos > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                              lbProblemSolved = False
                            End If

                       If Not lbProblemSolved Then
                          If m_scanner.StartPos > Me.MaxDistance Then
                            Me.MaxDistance = m_scanner.StartPos
                            max_tree = m_tree.clone
                          End If
                          m_scanner.StartPos = liMaxRange
                          Return False
                       Else
                          liMaxRange = m_scanner.EndPos
                          If liMaxRange > Me.MaxDistance Then
                            Me.MaxDistance = m_scanner.StartPos
                            max_tree = m_tree.clone
                          End If
                       End If
                       If m_tree.Errors.Count > 0 Then
                          Return False
                       End If

                Case TokenType.REFERENCES
            lbProblemSolved =                     ParseforeignKeyConstraint(node) ' NonTerminal Rule: foreignKeyConstraint
                    If m_tree.Errors.Count > 0 Then
                      If m_scanner.EndPos > Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree = m_tree.clone
                      End If
                      lbProblemSolved = False
                    End If

                Case Else
                If m_tree.Errors.Count = 0 Then
                lbProblemSolved = False
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWDCONSTRAINT.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEYWDCONSTRAINT"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEYWDCONSTRAINT.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "REFERENCES"))
                End If
                    m_tree.Errors.Add(new ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found.", &H0002, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos))
                    Exit Select
            End Select ' Choice Rule
                If Not lbProblemSolved Then
                   m_tree.Errors.Clear
                If Not lbProblemSolved Then
                  m_tree.Errors.Clear
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
            lbProblemSolved =                 ParseforeignKeyConstraint(parent.Nodes(parent.Nodes.Count -1)) ' NonTerminal Rule: foreignKeyConstraint
                If m_tree.Errors.Count > 0 Then
                  If m_scanner.EndPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  lbProblemSolved = False
                Else If m_scanner.EndPos = Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.EndPos
                    max_tree = m_tree.clone
                End If

                End If
                End If
             If (m_tree.Errors.Count > 0) Or Not lbProblemSolved Then
                 parent.Nodes.Remove(node)
                 Return False
             End If

            If m_scanner.Input.Length > (parent.Token.EndPos + 1) Then
            End If
            Finally
                If lbProblemSolved Then
                    parent.Token.UpdateRange(node.Token)
                    Me.MaxDistance = node.Token.EndPos
                    If m_scanner.EndPos >= Me.MaxDistance Then
                       If m_tree.MaxDistance > max_tree.MaxDistance Then
                          Me.MaxDistance = m_scanner.StartPos
                          max_tree = m_tree.clone
                       End If
                    End If
                Else
                   m_scanner.StartPos = liOriginalRange
                   parent.Nodes.Remove(node)
                End If
            End Try
            Return lbProblemSolved
        End Function ' NonTerminalSymbol: CONSTRAINTDEFINITION

        Private Function ParseprimaryKeyConstraint(ByVal parent As ParseNode) As Boolean ' NonTerminalSymbol: primaryKeyConstraint
            Dim tok As Token
            Dim n As ParseNode
            Dim node As ParseNode = parent.CreateNode(m_scanner.GetToken(TokenType.primaryKeyConstraint), "primaryKeyConstraint")
            Dim lbProblemSolved As Boolean = True

            Dim liOriginalRange as Integer = m_scanner.StartPos
            Dim liMaxRange as Integer = liOriginalRange
            parent.Nodes.Add(node)

            Try

             ' Concat Rule
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.PRIMARY) ' Terminal Rule: PRIMARY
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.PRIMARY.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "PRIMARY"))
                    End If
                    If tok.Type <> TokenType.PRIMARY Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.PRIMARY.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "PRIMARY"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.PRIMARY.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "PRIMARY"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If

             ' Concat Rule
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.KEY) ' Terminal Rule: KEY
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEY.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEY"))
                    End If
                    If tok.Type <> TokenType.KEY Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEY.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEY"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.KEY.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "KEY"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If

             ' Concat Rule
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.LPAREN) ' Terminal Rule: LPAREN
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.LPAREN.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "LPAREN"))
                    End If
                    If tok.Type <> TokenType.LPAREN Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.LPAREN.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "LPAREN"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.LPAREN.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "LPAREN"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If

             ' Concat Rule
            lbProblemSolved =                     ParseprimaryKeyColumnList(node) ' NonTerminal Rule: primaryKeyColumnList
                    If m_tree.Errors.Count > 0 Then
                      If m_scanner.EndPos > Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree = m_tree.clone
                      End If
                      lbProblemSolved = False
                    End If

               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If

             ' Concat Rule
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.RPAREN) ' Terminal Rule: RPAREN
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.RPAREN.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "RPAREN"))
                    End If
                    If tok.Type <> TokenType.RPAREN Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.RPAREN.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "RPAREN"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.RPAREN.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "RPAREN"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If
               If m_tree.Errors.Count > 0 Then
                  Return False
               End If

            If m_scanner.Input.Length > (parent.Token.EndPos + 1) Then
            End If
            Finally
                If lbProblemSolved Then
                    parent.Token.UpdateRange(node.Token)
                    Me.MaxDistance = node.Token.EndPos
                    If m_scanner.EndPos >= Me.MaxDistance Then
                       If m_tree.MaxDistance > max_tree.MaxDistance Then
                          Me.MaxDistance = m_scanner.StartPos
                          max_tree = m_tree.clone
                       End If
                    End If
                Else
                   m_scanner.StartPos = liOriginalRange
                   parent.Nodes.Remove(node)
                End If
            End Try
            Return lbProblemSolved
        End Function ' NonTerminalSymbol: primaryKeyConstraint

        Private Function ParseprimaryKeyColumnList(ByVal parent As ParseNode) As Boolean ' NonTerminalSymbol: primaryKeyColumnList
            Dim tok As Token
            Dim n As ParseNode
            Dim node As ParseNode = parent.CreateNode(m_scanner.GetToken(TokenType.primaryKeyColumnList), "primaryKeyColumnList")
            Dim lbProblemSolved As Boolean = True

            Dim liOriginalRange as Integer = m_scanner.StartPos
            Dim liMaxRange as Integer = liOriginalRange
            parent.Nodes.Add(node)

            Try

             ' Concat Rule
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.IDENTIFIER) ' Terminal Rule: IDENTIFIER
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.IDENTIFIER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                    End If
                    If tok.Type <> TokenType.IDENTIFIER Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.IDENTIFIER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.IDENTIFIER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If

             ' Concat Rule
                                tok = m_scanner.LookAhead(TokenType.COMMA) ' ZeroOrMore Rule
                    While tok.Type = TokenType.COMMA
                    m_tree.Errors.Clear

                         ' Concat Rule
                                                        lbProblemSolved = True
                                                                tok = m_scanner.Scan(TokenType.COMMA) ' Terminal Rule: COMMA
                                n = node.CreateNode(tok, tok.ToString() )
                                node.Token.UpdateRange(tok)
                                node.Nodes.Add(n)
                                If m_scanner.StartPos >= Me.MaxDistance Then
                                  m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.COMMA.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "COMMA"))
                                End If
                                If tok.Type <> TokenType.COMMA Then
                                  m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.COMMA.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "COMMA"))
                                  lbProblemSolved = False
                                  If liMaxRange >= Me.MaxDistance Then
                                    Me.MaxDistance = m_scanner.StartPos
                                    max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.COMMA.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "COMMA"))
                                    max_tree = m_tree.clone
                                  End If
                                  Return False

                                Else
                                If m_scanner.StartPos >= Me.MaxDistance Then
                                    m_tree.Optionals.Clear
                                End If
                                End If


                           If Not lbProblemSolved Then
                              If m_scanner.StartPos > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                              m_scanner.StartPos = liMaxRange
                              Return False
                           Else
                              liMaxRange = m_scanner.EndPos
                              If liMaxRange > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                           End If

                         ' Concat Rule
                                                        lbProblemSolved = True
                                                                tok = m_scanner.Scan(TokenType.IDENTIFIER) ' Terminal Rule: IDENTIFIER
                                n = node.CreateNode(tok, tok.ToString() )
                                node.Token.UpdateRange(tok)
                                node.Nodes.Add(n)
                                If m_scanner.StartPos >= Me.MaxDistance Then
                                  m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.IDENTIFIER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                                End If
                                If tok.Type <> TokenType.IDENTIFIER Then
                                  m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.IDENTIFIER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                                  lbProblemSolved = False
                                  If liMaxRange >= Me.MaxDistance Then
                                    Me.MaxDistance = m_scanner.StartPos
                                    max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.IDENTIFIER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                                    max_tree = m_tree.clone
                                  End If
                                  Return False

                                Else
                                If m_scanner.StartPos >= Me.MaxDistance Then
                                    m_tree.Optionals.Clear
                                End If
                                End If


                           If Not lbProblemSolved Then
                              If m_scanner.StartPos > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                              m_scanner.StartPos = liMaxRange
                              Return False
                           Else
                              liMaxRange = m_scanner.EndPos
                              If liMaxRange > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                           End If
                           If m_tree.Errors.Count > 0 Then
                              Return False
                           End If
                    tok = m_scanner.LookAhead(TokenType.COMMA) ' ZeroOrMore Rule
                    If Not lbProblemSolved Then Exit While
                    End While
            If m_tree.Errors.Count > 0 Then
                                Return False
            End If

               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If
               If m_tree.Errors.Count > 0 Then
                  Return False
               End If

            If m_scanner.Input.Length > (parent.Token.EndPos + 1) Then
            End If
            Finally
                If lbProblemSolved Then
                    parent.Token.UpdateRange(node.Token)
                    Me.MaxDistance = node.Token.EndPos
                    If m_scanner.EndPos >= Me.MaxDistance Then
                       If m_tree.MaxDistance > max_tree.MaxDistance Then
                          Me.MaxDistance = m_scanner.StartPos
                          max_tree = m_tree.clone
                       End If
                    End If
                Else
                   m_scanner.StartPos = liOriginalRange
                   parent.Nodes.Remove(node)
                End If
            End Try
            Return lbProblemSolved
        End Function ' NonTerminalSymbol: primaryKeyColumnList

        Private Function ParseforeignKeyConstraint(ByVal parent As ParseNode) As Boolean ' NonTerminalSymbol: foreignKeyConstraint
            Dim tok As Token
            Dim n As ParseNode
            Dim node As ParseNode = parent.CreateNode(m_scanner.GetToken(TokenType.foreignKeyConstraint), "foreignKeyConstraint")
            Dim lbProblemSolved As Boolean = True

            Dim liOriginalRange as Integer = m_scanner.StartPos
            Dim liMaxRange as Integer = liOriginalRange
            parent.Nodes.Add(node)

            Try

             ' Concat Rule
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.REFERENCES) ' Terminal Rule: REFERENCES
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.REFERENCES.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "REFERENCES"))
                    End If
                    If tok.Type <> TokenType.REFERENCES Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.REFERENCES.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "REFERENCES"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.REFERENCES.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "REFERENCES"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If

             ' Concat Rule
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.IDENTIFIER) ' Terminal Rule: IDENTIFIER
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.IDENTIFIER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                    End If
                    If tok.Type <> TokenType.IDENTIFIER Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.IDENTIFIER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.IDENTIFIER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If

             ' Concat Rule
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.LPAREN) ' Terminal Rule: LPAREN
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.LPAREN.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "LPAREN"))
                    End If
                    If tok.Type <> TokenType.LPAREN Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.LPAREN.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "LPAREN"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.LPAREN.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "LPAREN"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If

             ' Concat Rule
            lbProblemSolved =                     ParseforeignKeyColumnList(node) ' NonTerminal Rule: foreignKeyColumnList
                    If m_tree.Errors.Count > 0 Then
                      If m_scanner.EndPos > Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree = m_tree.clone
                      End If
                      lbProblemSolved = False
                    End If

               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If

             ' Concat Rule
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.RPAREN) ' Terminal Rule: RPAREN
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.RPAREN.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "RPAREN"))
                    End If
                    If tok.Type <> TokenType.RPAREN Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.RPAREN.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "RPAREN"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.RPAREN.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "RPAREN"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If
               If m_tree.Errors.Count > 0 Then
                  Return False
               End If

            If m_scanner.Input.Length > (parent.Token.EndPos + 1) Then
            End If
            Finally
                If lbProblemSolved Then
                    parent.Token.UpdateRange(node.Token)
                    Me.MaxDistance = node.Token.EndPos
                    If m_scanner.EndPos >= Me.MaxDistance Then
                       If m_tree.MaxDistance > max_tree.MaxDistance Then
                          Me.MaxDistance = m_scanner.StartPos
                          max_tree = m_tree.clone
                       End If
                    End If
                Else
                   m_scanner.StartPos = liOriginalRange
                   parent.Nodes.Remove(node)
                End If
            End Try
            Return lbProblemSolved
        End Function ' NonTerminalSymbol: foreignKeyConstraint

        Private Function ParseforeignKeyColumnList(ByVal parent As ParseNode) As Boolean ' NonTerminalSymbol: foreignKeyColumnList
            Dim tok As Token
            Dim n As ParseNode
            Dim node As ParseNode = parent.CreateNode(m_scanner.GetToken(TokenType.foreignKeyColumnList), "foreignKeyColumnList")
            Dim lbProblemSolved As Boolean = True

            Dim liOriginalRange as Integer = m_scanner.StartPos
            Dim liMaxRange as Integer = liOriginalRange
            parent.Nodes.Add(node)

            Try

             ' Concat Rule
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.IDENTIFIER) ' Terminal Rule: IDENTIFIER
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.IDENTIFIER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                    End If
                    If tok.Type <> TokenType.IDENTIFIER Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.IDENTIFIER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.IDENTIFIER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If

             ' Concat Rule
                                tok = m_scanner.LookAhead(TokenType.COMMA) ' ZeroOrMore Rule
                    While tok.Type = TokenType.COMMA
                    m_tree.Errors.Clear

                         ' Concat Rule
                                                        lbProblemSolved = True
                                                                tok = m_scanner.Scan(TokenType.COMMA) ' Terminal Rule: COMMA
                                n = node.CreateNode(tok, tok.ToString() )
                                node.Token.UpdateRange(tok)
                                node.Nodes.Add(n)
                                If m_scanner.StartPos >= Me.MaxDistance Then
                                  m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.COMMA.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "COMMA"))
                                End If
                                If tok.Type <> TokenType.COMMA Then
                                  m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.COMMA.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "COMMA"))
                                  lbProblemSolved = False
                                  If liMaxRange >= Me.MaxDistance Then
                                    Me.MaxDistance = m_scanner.StartPos
                                    max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.COMMA.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "COMMA"))
                                    max_tree = m_tree.clone
                                  End If
                                  Return False

                                Else
                                If m_scanner.StartPos >= Me.MaxDistance Then
                                    m_tree.Optionals.Clear
                                End If
                                End If


                           If Not lbProblemSolved Then
                              If m_scanner.StartPos > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                              m_scanner.StartPos = liMaxRange
                              Return False
                           Else
                              liMaxRange = m_scanner.EndPos
                              If liMaxRange > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                           End If

                         ' Concat Rule
                                                        lbProblemSolved = True
                                                                tok = m_scanner.Scan(TokenType.IDENTIFIER) ' Terminal Rule: IDENTIFIER
                                n = node.CreateNode(tok, tok.ToString() )
                                node.Token.UpdateRange(tok)
                                node.Nodes.Add(n)
                                If m_scanner.StartPos >= Me.MaxDistance Then
                                  m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.IDENTIFIER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                                End If
                                If tok.Type <> TokenType.IDENTIFIER Then
                                  m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.IDENTIFIER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                                  lbProblemSolved = False
                                  If liMaxRange >= Me.MaxDistance Then
                                    Me.MaxDistance = m_scanner.StartPos
                                    max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.IDENTIFIER.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "IDENTIFIER"))
                                    max_tree = m_tree.clone
                                  End If
                                  Return False

                                Else
                                If m_scanner.StartPos >= Me.MaxDistance Then
                                    m_tree.Optionals.Clear
                                End If
                                End If


                           If Not lbProblemSolved Then
                              If m_scanner.StartPos > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                              m_scanner.StartPos = liMaxRange
                              Return False
                           Else
                              liMaxRange = m_scanner.EndPos
                              If liMaxRange > Me.MaxDistance Then
                                Me.MaxDistance = m_scanner.StartPos
                                max_tree = m_tree.clone
                              End If
                           End If
                           If m_tree.Errors.Count > 0 Then
                              Return False
                           End If
                    tok = m_scanner.LookAhead(TokenType.COMMA) ' ZeroOrMore Rule
                    If Not lbProblemSolved Then Exit While
                    End While
            If m_tree.Errors.Count > 0 Then
                                Return False
            End If

               If Not lbProblemSolved Then
                  If m_scanner.StartPos > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                  m_scanner.StartPos = liMaxRange
                  Return False
               Else
                  liMaxRange = m_scanner.EndPos
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
               End If
               If m_tree.Errors.Count > 0 Then
                  Return False
               End If

            If m_scanner.Input.Length > (parent.Token.EndPos + 1) Then
            End If
            Finally
                If lbProblemSolved Then
                    parent.Token.UpdateRange(node.Token)
                    Me.MaxDistance = node.Token.EndPos
                    If m_scanner.EndPos >= Me.MaxDistance Then
                       If m_tree.MaxDistance > max_tree.MaxDistance Then
                          Me.MaxDistance = m_scanner.StartPos
                          max_tree = m_tree.clone
                       End If
                    End If
                Else
                   m_scanner.StartPos = liOriginalRange
                   parent.Nodes.Remove(node)
                End If
            End Try
            Return lbProblemSolved
        End Function ' NonTerminalSymbol: foreignKeyColumnList

        Private Function ParseStart(ByVal parent As ParseNode) As Boolean ' NonTerminalSymbol: Start
            Dim tok As Token
            Dim n As ParseNode
            Dim node As ParseNode = parent.CreateNode(m_scanner.GetToken(TokenType.Start), "Start")
            Dim lbProblemSolved As Boolean = True

            Dim liOriginalRange as Integer = m_scanner.StartPos
            Dim liMaxRange as Integer = liOriginalRange
            parent.Nodes.Add(node)

            Try
            tok = m_scanner.LookAhead({TokenType.CREATE, TokenType.EOF}) ' Choice Rule
            
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.CREATE.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "CREATE"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.CREATE.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "EOF"))
            Select Case tok.Type
             ' Choice Rule
                Case TokenType.CREATE
            lbProblemSolved =                     Parseddl(node) ' NonTerminal Rule: ddl
                    If m_tree.Errors.Count > 0 Then
                      If m_scanner.EndPos > Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree = m_tree.clone
                      End If
                      lbProblemSolved = False
                    End If

                Case TokenType.EOF
                                lbProblemSolved = True
                                        tok = m_scanner.Scan(TokenType.EOF) ' Terminal Rule: EOF
                    n = node.CreateNode(tok, tok.ToString() )
                    node.Token.UpdateRange(tok)
                    node.Nodes.Add(n)
                    If m_scanner.StartPos >= Me.MaxDistance Then
                      m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.EOF.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "EOF"))
                    End If
                    If tok.Type <> TokenType.EOF Then
                      m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.EOF.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "EOF"))
                      lbProblemSolved = False
                      If liMaxRange >= Me.MaxDistance Then
                        Me.MaxDistance = m_scanner.StartPos
                        max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.EOF.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "EOF"))
                        max_tree = m_tree.clone
                      End If
                      Return False

                    Else
                    If m_scanner.StartPos >= Me.MaxDistance Then
                        m_tree.Optionals.Clear
                    End If
                    End If


                Case Else
                If m_tree.Errors.Count = 0 Then
                lbProblemSolved = False
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.CREATE.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "CREATE"))
                m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.CREATE.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "EOF"))
                End If
                    m_tree.Errors.Add(new ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found.", &H0002, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos))
                    Exit Select
            End Select ' Choice Rule
                If Not lbProblemSolved Then
                   m_tree.Errors.Clear
                If Not lbProblemSolved Then
                  m_tree.Errors.Clear
                  If liMaxRange > Me.MaxDistance Then
                    Me.MaxDistance = m_scanner.StartPos
                    max_tree = m_tree.clone
                  End If
                        lbProblemSolved = True
                        tok = m_scanner.Scan(TokenType.EOF) ' Terminal Rule: EOF
            n = node.CreateNode(tok, tok.ToString() )
            node.Token.UpdateRange(tok)
            node.Nodes.Add(n)
            If m_scanner.StartPos >= Me.MaxDistance Then
              m_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.EOF.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "EOF"))
            End If
            If tok.Type <> TokenType.EOF Then
              m_tree.Errors.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.EOF.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "EOF"))
              lbProblemSolved = False
              If liMaxRange >= Me.MaxDistance Then
                Me.MaxDistance = m_scanner.StartPos
                max_tree.Optionals.Add(New ParseError("Unexpected token '" + tok.Text.Replace("\n", "") + "' found. Expected " + TokenType.EOF.ToString(), &H1001, 0, tok.StartPos, tok.StartPos, tok.EndPos - tok.StartPos, "EOF"))
                max_tree = m_tree.clone
              End If
              Return False

            Else
            If m_scanner.StartPos >= Me.MaxDistance Then
                m_tree.Optionals.Clear
            End If
            End If


                End If
                End If
             If (m_tree.Errors.Count > 0) Or Not lbProblemSolved Then
                 parent.Nodes.Remove(node)
                 Return False
             End If

            If m_scanner.Input.Length > (parent.Token.EndPos + 1) Then
            End If
            Finally
                If lbProblemSolved Then
                    parent.Token.UpdateRange(node.Token)
                    Me.MaxDistance = node.Token.EndPos
                    If m_scanner.EndPos >= Me.MaxDistance Then
                       If m_tree.MaxDistance > max_tree.MaxDistance Then
                          Me.MaxDistance = m_scanner.StartPos
                          max_tree = m_tree.clone
                       End If
                    End If
                Else
                   m_scanner.StartPos = liOriginalRange
                   parent.Nodes.Remove(node)
                End If
            End Try
            Return lbProblemSolved
        End Function ' NonTerminalSymbol: Start


    End Class
#End Region
End Namespace

